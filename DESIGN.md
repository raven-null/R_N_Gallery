# 基于 Netlify Blobs 的网页图库 · 设计文档

> 版本：v0.9.2 ｜ 状态：已整合 + 本地检测通过 ｜ 目标部署：Netlify + Blobs

## 本地检测结果（v0.9.2，全部通过）

- 新增 **`scripts/dev-server.js`**：内置开发服务器（静态文件 + `/api/*` 调用真实 Function handler + 内存 Blobs 模拟），`npm run dev` → http://localhost:8787，无需 netlify-cli/网络；
- API 全链路测试通过：列表 / 单张 / raw 字节输出（MIME 与文件头校验）/ 上传（PNG 尺寸解析）/ PATCH / DELETE 单张 / 清空 / stats（计数+字节+月份分布）/ export / import（webp 600×899 尺寸解析正确）；
- 修复：① `/api/meta/stats` 被单张查询路由误拦截（补 `startsWith("/api/photos/")` 限定）；② import 拉取静态图时本地环境用 http、线上用 https 的协议判断；③ `IMAGES` 数组 3 个磁盘缺失的引用（640.png、148749850…、143255789…）已清理，与磁盘 243 文件完全对齐；④ 新增 `scripts/check-images.js` 完整性检查脚本。

## 整合状态（v0.9，制作完成）

- **项目结构**：`index.html`（SPA 主壳）+ `assets/` + `image/`（静态图源）+ `netlify/functions/`（统一 API）+ `netlify.toml` + `package.json` + `README.md`；遗留独立页（upload/settings/detail）已删除；
- **后端**：`photos.js` 统一入口处理全部 `/api/*`（列表/单张/raw 图片输出/上传/更新/删除/清空/统计/导出/导入），`_lib.js` 共享（Blobs store、`ADMIN_TOKEN`/`UPLOAD_TOKEN` 鉴权、webp/jpeg/png/gif 尺寸解析）；
- **Blob key**：`img/<id>`（原图字节）+ `meta/<id>.json`（元数据）；
- **前端数据源**：优先拉取 `/api/photos`（URL 指向 `/api/photos/<id>/raw`），API 不可用时回退 `image/` 静态列表（本地 file:// 预览可用）；上传为真实流程（canvas 压缩 → XHR 带进度 → Blobs）；设置页接入真实统计/导入/导出/清空，token 存 localStorage；
- **导入流程**：部署后 → 设置窗口 →「导入静态图片到 Blobs」，将 `image/` 写入 Blobs（函数从站点同源 URL 拉取并解析尺寸），导入后图库自动切换数据源。

## 部署指引（需用户执行）

1. `git push -u origin main`（推送 GitHub 仓库 `raven-null/R_N_Gallery`，需有效 token/凭据）；
2. Netlify → Import from GitHub → 选择仓库（自动读取 `netlify.toml`，publish=`.`、无需构建）；
3. 可选配置环境变量 `ADMIN_TOKEN`（设置页填写同一值作访问令牌）；
4. 访问站点 → 设置 → 导入静态图片 → 图库数据进入 Blobs；
5. 可选：删除 `image/` 目录释放仓库体积。

## 变更日志

- **v0.9.3（图库瀑布流全宽铺满）**
  - 图库页解除 `max-width: 1280px` 限制，瀑布流**铺满整个视口宽度**；
  - 列布局由「固定 4 列」改为「仅指定列宽 240px」——浏览器按视口宽度自动生成列数（宽屏更多列、窄屏自动减少），任何分辨率下都横向铺满、无左右空位。
- **v0.8.11（图片出现动画：刷新交错浮现 + 滚动视口触发）**
  - 图库卡片与搜索结果卡片改为**视口触发式出现动画**（IntersectionObserver，双向滚动生效）：淡入 + 上移 + 轻微缩放 + **模糊消散**（blur 6px→0），0.55s `--ease-out`，苹果照片应用同款质感；
  - **刷新/首次渲染**：首批卡片进入视口时按顺序**交错浮现**（每批 12 张、每张 40ms stagger）；
  - **滚动加载**：新卡片滚入视口（提前 120px 触发）才播放动画，上下滚动均生效；过渡完成后自动清除 delay，不影响 hover 动画；
  - `prefers-reduced-motion` 下直接显示；搜索窗口结果同步接入；拖拽区/详情页保留原 `card-in` 入场。
- **v0.8.10（菜单关闭交互与图标修复）**
  - **移除"点击固定"逻辑**：此前点击按钮会固定菜单（鼠标移开不关闭），改为点击 = 开关切换；现在悬停打开、**移开鼠标 180ms 即正常关闭**，点击外部 / Esc 同样关闭；
  - **标签按钮图标保持不变**：打开标签菜单时不再切换为 ✕（仅保留橙色高亮），页面切换按钮行为不变。
- **v0.8.9（按钮组与菜单展开状态联动）**
  - 修复：鼠标移到弹出菜单（页面切换 / 标签）上时，`fab-group` 失去 hover 导致子按钮组收起；
  - 改为 JS 维护展开计数：**任一菜单打开期间按钮组保持展开**（`fab-group.active`），全部菜单关闭后才收起；重复触发有防抖计数，不会错乱。
- **v0.8.8（标签菜单支持内部滚动）**
  - 标签菜单卡片限制高度（`max-height: min(62vh, 440px)`），标签多/矮屏时**内部向下滚动**，不再超出视口；
  - `overscroll-behavior: contain` 防止滚动穿透到页面。
- **v0.8.7（标签菜单改黑色毛玻璃）**
  - 标签弹出菜单从「透明玻璃」改为**黑色毛玻璃卡片**：容器整体加深色半透明渐变 + `backdrop-filter` 模糊 + 白色描边/内高光 + 大阴影，菜单项在卡片内 hover 提亮；
  - 全站残留的蓝灰色玻璃（页面切换菜单项、悬浮按钮、灯箱按钮与信息面板等）统一为黑色系，视觉完全一致。
- **v0.8.6（搜索独立为窗口界面）**
  - **删除标签菜单中的搜索按钮与输入框**，搜索升级为**独立悬浮窗口**（与上传/设置同款：Genie 喷出、可参与文件堆层叠）；
  - 搜索窗口内容：大号搜索框（放大镜图标 + 清除按钮）+ 结果计数 + 方形网格结果（方形裁切卡片，hover 显示标题/标签）+ 空态提示；输入即时过滤，点击结果打开灯箱；
  - 页面切换菜单新增「搜索」项（放大镜图标，stagger 扩展至 4 项）；`/` 快捷键改为打开搜索窗口并聚焦；
  - 灯箱打开逻辑提取为全局 `openLightboxById()`，图库与搜索窗口共用；
  - 标签菜单回归纯粹（仅主要标签 Top 8 + 全部）。
- **v0.8.5（玻璃改版：半透明白色玻璃 → 黑色毛玻璃）**
  - 全站玻璃元素由「半透明白色渐变」改为「**黑色毛玻璃**」：深色半透明渐变（约 0.7~0.96 不透明度）+ 保留 `backdrop-filter` 模糊，类似 macOS 深色模式毛玻璃；
  - 变量 `--glass/--glass-strong/--bg-soft` 与全部硬编码浅色背景（按钮/卡片/表单/菜单/进度条/滚动条等）统一替换为黑色系；白色半透明**描边与内高光**保留（毛玻璃特征）；灯箱文字渐变、按钮光泽层等前景效果不受影响；
  - 阴影加深、背景底色微调（`#0a0c12 → #08090d`）、饱和率 180%→160% 更沉稳。
- **v0.8.4（标签侧边栏改为悬浮弹出菜单，搜索改按钮）**
  - **删除侧边栏抽屉**（drawer-mask / side-drawer / tag-row / searchbar 等样式与结构一并清理），标签筛选改为**与页面切换菜单同款悬浮弹出**：悬停/点击标签按钮即从按钮位置弹性长出（液态玻璃卡片），标签行 stagger 逐个浮现；
  - 菜单只展示**主要标签**（按图片数量 Top 8）+「全部」行，选中后菜单收起、按钮亮橙色角标；
  - **搜索改为按钮**：菜单头部放大镜按钮，点击后输入框平滑展开（grid 行高过渡），输入即时过滤（与标签叠加）；`/` 快捷键改为直接打开标签菜单并聚焦搜索框；
  - 弹出交互抽为通用 `initFlyout()`（悬停弹出 + 点击固定 + 180ms 延迟收起 + 外部/Esc 关闭），页面切换菜单同步复用；
  - 遗留独立页（upload/settings/detail）中的旧抽屉结构未再清理（文件本身已无入口）。
- **v0.8.3（页面切换菜单悬停弹出 + 苹果感动画）**
  - 页面切换按钮（fab-page）支持**悬停弹出**菜单：鼠标移入即弹出、移出（含按钮与菜单区域）延迟 180ms 收起，路径无缝隙不抖断；点击可**固定**菜单（移动端无悬停时的主要交互），点外部关闭；
  - 弹出动画升级：菜单从按钮位置**弹性"长"出**（`transform-origin: right bottom` + `--ease-spring` 过冲），三个菜单项**逐个浮现**（stagger 40/90/140ms，下滑 + 淡入 + 缩放）；
  - 点击菜单项后自动收起菜单。
- **v0.8.2（Bug 修复）**
  - 修复 `openWindow` 中残留的旧常量 `STACK_GAP`（v0.8.1 重命名后未清理）导致的 `ReferenceError`，窗口无法打开；窗口位置现统一由 `refreshStack` 排布。
- **v0.8.1（窗口堆叠改为散落文件堆效果）**
  - 参考用户示例（`E:\学习\UI训练\文件堆叠.html`），窗口堆叠从「平行偏移」改为**带旋转角度的文件堆**：顶层窗口端正（轻微 -1°），被压窗口按深度**交替方向旋转**（±1.6°~5°封顶）+ 向外散开（每层 14px，移动端 8px），视觉如随手叠起的纸张；
  - 位置/角度由 CSS 变量 `--win-x / --win-y / --win-r` 驱动，打开/关闭/置顶时其余窗口经 CSS `transition` 平滑移动归位；
  - Genie 喷出/吸入动画不变（动画期间旋转为 0，结束平滑过渡到目标角度）。
- **v0.8（上传/设置改为悬浮窗口，多窗口层叠）**
  - 上传、设置从「全屏面板」改为**悬浮窗口**：居中于图库页上方（约 880×76vh），圆角 + 液态玻璃边框 + 大阴影，图库页保持可见（无遮罩）；
  - **多窗口层叠**：先开上传再开设置时，两个窗口**错位堆叠**（新窗口向右下偏移 22px，移动端 10px），后开的压在前窗之上；被压住的窗口轻微后移、变暗（`.dimmed`，像叠起的文件）；
  - 窗口交互：点击窗口**置顶**（z-index 提升 + 菜单高亮跟随）；macOS **交通灯标题栏**（红点关闭窗口，黄/绿装饰）；返回按钮/红点/Esc 关闭窗口；关闭动画**串行排队**，避免多个 Genie 滤镜并发卡顿；
  - 菜单「图库」= 关闭全部窗口（逐个吸入）；Genie 喷出/吸入动画与性能优化（v0.7/v0.7.1）全部沿用，目标位置改为窗口居中坐标；
  - 移除 `stack-1` 层叠样式（被窗口错位机制取代）。
- **v0.7.1（Genie 动画性能优化）**
  - 修复上传 → 设置切换时的动画卡顿：SVG 位移滤镜此前逐帧作用于整个全屏面板（含内部多层毛玻璃），重绘开销过大；
  - 优化措施：滤镜改为**分段启用**（喷出仅前 42%、吸入仅后 45%），滤镜帧数减半以上，其余帧只走 GPU 合成的 transform/opacity；
  - 波浪包络在滤镜开关点**归零**，切换滤镜无视觉跳变；
  - 动画期间通过 `.page-panel.genie` 规则**临时禁用面板内全部嵌套 `backdrop-filter`**（动画结束自动恢复），消除滤镜容器内的多重模糊；
  - 面板加 `contain: layout paint` 隔离重排/重绘范围；缩短动画时长（喷出 480ms / 吸入 360ms）、减小波浪幅度。
- **v0.7（页面切换动画改为 macOS Genie 效果）**
  - 上传/设置面板切换从「CSS 缩放过渡」升级为 **Genie Effect（精灵效果）**：窗口从右下角悬浮按钮位置**喷出**展开、关闭时**吸入**收回，边缘带波浪形变；
  - 实现：内联 SVG 滤镜（`feTurbulence` + `feDisplacementMap`）逐帧驱动（rAF），进入时弹性过冲（`easeOutBack`）且波浪渐弱，退出时波浪增强模拟吸入感；
  - 面板动画由 CSS transition 改为 JS 驱动（transform / filter / opacity），动画结束即移除滤镜避免常驻开销；
  - 移除面板间 `stack-1` 层叠显示逻辑（CSS 类保留，JS 不再使用）；面板 → 面板切换为先吸入旧面板、再喷出新面板；
  - `prefers-reduced-motion` 下直接切换、无动画。
- **v0.6（SPA 化 + 真实图片，用户大幅重构）**
  - **单页应用架构**：上传、设置从独立页面改为 `index.html` 内的全屏面板（`.page-panel`），macOS 风格「右下角缩放展开 / 收入」动画（`transform-origin: right bottom` + 弹性曲线 `cubic-bezier(0.2,0.8,0.2,1)`），面板间支持 `stack-1` 层叠效果；`upload.html / settings.html / detail.html` 保留为遗留文件，不再作为导航入口；
  - **真实图片数据**：新增 `html/image/` 目录（243 张 webp/jpg/png），`app.js` 中以 `IMAGES` 数组硬编码路径替换原有 SVG 渐变占位图；
  - **悬浮按钮组**：单小球升级为 `fab-group` —— 主按钮（三圆点图标，44px，呼吸光晕，悬停/激活旋转 90° 展开子按钮）+ 两个子按钮（标签筛选 38px、页面切换 38px，均为用户设定尺寸）；
  - **页面切换菜单**：页面按钮点击弹出液态玻璃下拉菜单（图库 / 上传 / 设置，SVG 图标，当前页高亮），点击外部关闭；
  - **灯箱增强**：新增右侧信息面板 `.lb-info`（渐变标题、日期 / 大小 / 分辨率 / 格式、标签胶囊），图片与信息并排展示；
  - 侧边栏抽屉简化为纯「搜索 + 标签」；导航职责移交页面切换菜单；
  - 视觉细节：新增 `--ease-spring` / `--ease-out` 弹性曲线、`--glass-refraction` 折射内阴影、卡片入场 `card-in`、卡片悬停标签胶囊滑入、灯箱信息面板 `slide-up` 等；
  - 上传面板标签输入改用独立 id（`tagListUpload` / `tagInputUpload`），避免与侧边栏 `tagList` 冲突。
- **v0.5（导航并入侧边栏，用户设定小球尺寸）**
  - **取消顶部导航栏**（全部页面），导航入口（图库 / 上传 / 设置）统一收进**右侧侧边栏抽屉**：图库页抽屉 = 导航 + 搜索框 + 标签列表；其余页面抽屉 = 导航；
  - 悬浮小球按**用户设定改为 35px**，图标从 emoji 改为**内联 SVG**（标签/✕ 双层叠放，绝对居中），开合时交叉淡入 + 旋转切换，解决小尺寸下图标不居中的问题；
  - 删除 `.topbar/.topnav` 样式与滚动加深逻辑；抽屉开关逻辑抽为全站通用 `initDrawer()`；
  - 全局 Esc / 遮罩 / ✕ 收起；搜索框移入抽屉后宽度自适应。
- **v0.4（标签移入侧边栏）**
  - 标签筛选从首页移除后，改为**右侧滑入式侧边栏**（液态玻璃抽屉）：全部标签按数量倒序排列，点击筛选、再次点击取消，与搜索关键词**叠加过滤**；
  - 新增**悬浮小球**（右下角液态玻璃圆钮，呼吸光晕动画）：点击展开/收起侧边栏，展开时旋转 180° 变为 ✕；选中标签后小球出现橙色角标；
  - 侧边栏动效：`translateX` 弹性滑入 + 遮罩淡入；支持点遮罩 / 抽屉内 ✕ / Esc 收起；标签行悬停左移、选中高亮；
  - 修复：补回被 v0.3 清理误删的 `@keyframes pop`（标签行角标等动画依赖）。
- **v0.3（首页精简，用户删改）**
  - 图库墙改为**极简布局**：移除页头标题区（"图库墙"标题、上传快捷按钮、筛选/随机按钮）与**整个标签筛选区**（含筛选按钮、状态提示条、标签 chips）；
  - 首页仅保留：导航（图库 / 上传 / 设置）+ 搜索框 + 瀑布流 + 灯箱；搜索仍为客户端即时过滤（标题 / 描述 / 标签）；
  - 同步清理 `app.js` 中失效的筛选代码与 `style.css` 中无引用的筛选样式；
  - 后端 API 的 `tag` 查询参数**保留备用**（前端 UI 未启用）。
- **v0.2（UI 改版）**
  - 图库墙标签筛选区改为**按需展开**：默认隐藏，点「筛选」按钮或输入搜索词时才出现，支持一键清除筛选；
  - **移除相册功能**：删除相册页面（`html/album.html`）与全部导航入口，元数据、API、上传/详情表单中的相册字段一并移除；
  - 顶部导航**移除品牌 Logo**（"留影"区块），导航仅保留功能入口；
  - 全站引入**苹果液态玻璃（Liquid Glass）视觉风格与动效**：毛玻璃层、内高光、动态光斑背景、入场/悬停/按压动画、灯箱与弹窗动效、滚动加深导航、`prefers-reduced-motion` 适配。
- **v0.1**：初版设计（架构、数据模型、API、五个页面设计稿）。

## 1. 项目概述

一个**零后端服务器**的私人网页图库：图片存储在 [Netlify Blobs](https://docs.netlify.com/build/data-and-storage/netlify-blobs/)（Netlify 提供的 key-value 对象存储）中，通过 Netlify Functions 提供读写 API，前端是纯静态页面。

**核心目标**

- 上传、浏览、搜索、管理个人图片（照片 / 截图 / 素材）
- 全站托管于 Netlify，无服务器实例、无数据库、无额外费用（免费额度内）
- 图片默认私有，未授权不可访问
- 移动端可用，观感现代

**非目标（本期不做）**

- 多用户 / 社交 / 评论
- 在线图片编辑
- 视频、大文件（>20MB）托管

---

## 2. 技术选型与理由

| 层 | 选型 | 理由 |
|---|---|---|
| 前端 | 原生 HTML/CSS/JS（或 Vite 构建） | 图库交互不复杂，无框架依赖，加载最快；后续可平滑迁移 Astro/SvelteKit |
| 存储 | Netlify Blobs | 与站点同生态、全球边缘复制、免运维；按套餐配额计费，个人图库免费额度足够 |
| 逻辑层 | Netlify Functions（Node + `@netlify/blobs`） | Blobs 只能通过 Functions / Edge Functions 访问，这是官方推荐路径 |
| 图片优化 | Netlify Image CDN（`/.netlify/images`） | 动态裁剪 / 转 WebP / 缓存，弥补"Blobs 无公开 URL、Functions 响应体积受限"的短板 |
| 鉴权 | 请求头 Token 比对环境变量（个人用） | 最简方案；多人用再换 Netlify Identity |
| 元数据 | Blob 内 JSON 文件（见 §4） | 单机个人使用量级（数百~数千张），JSON 元数据 + 运行时 list 完全够用，省去数据库 |

> ⚠️ 关键限制（设计必须绕开，具体数值以 [官方文档](https://docs.netlify.com/build/data-and-storage/netlify-blobs/) 为准）：
> - Blobs **没有公开 URL**，必须经 Function 读字节后输出；
> - Functions **同步响应体有体积上限（约 10MB）**，大图不能直接整包返回；
> - 单对象体积、元数据体积、站点存储配额均有限额。

---

## 3. 系统架构

```
┌──────────────┐  静态资源(CDN)   ┌───────────────────────────┐
│   浏览器      │ ───────────────► │  Netlify 静态站点 (页面)    │
│ (图库前端)    │                  └─────────────┬─────────────┘
└──────┬───────┘                                │
       │  /api/*                                │
       ▼                                        │ /.netlify/images?url=/api/…
┌───────────────────┐        ┌──────────────────▼───────────┐
│ Netlify Functions │        │ Netlify Image CDN（裁剪/转码/  │
│  (鉴权·读写·代理)  │        │  缓存，图片优化出口）           │
└─────────┬─────────┘        └──────────────────────────────┘
          │ @netlify/blobs
          ▼
┌───────────────────────────────────────────┐
│           Netlify Blobs Store "photos"     │
│  originals/<yyyymm>/<id>.<ext>   原图       │
│  thumbs/<yyyymm>/<id>.webp      缩略图      │
│  meta/<yyyymm>/<id>.json        元数据      │
│  index/tags/<tag>.json         标签索引     │
└───────────────────────────────────────────┘
```

读写链路：

- **读**：浏览器 → CDN（图片走 Image CDN）→ Function → Blobs → 字节流出；
- **写**：浏览器（已压缩）→ Function → Blobs 三个 key 一次写入；
- **私密性**：Blobs 本身私有，图片 URL 均由 Function 签发鉴权，CDN 缓存 key 不可枚举。

---

## 4. 数据模型

### 4.1 Blob key 规划（store 名：`photos`）

```
originals/<yyyymm>/<id>.<ext>   原始图片（唯一真源）
thumbs/<yyyymm>/<id>.webp       缩略图（前端生成，宽≤800px）
meta/<yyyymm>/<id>.json         该图元数据
index/tags/<tag>.json           标签 → 图片 id 列表（冗余索引）
```

- `id`：`nanoid` 风格短随机串（如 `k9f2x1`），**不可顺序枚举**，本身即一层访问保护；
- 按 `yyyymm` 分前缀：list 按月份分区，删除、清理、迁移都方便；
- Blob 自带 metadata（约 2KB 上限）存轻量信息（mime、width、height），重信息放 JSON。

### 4.2 元数据 JSON 结构

```json
{
  "id": "k9f2x1",
  "title": "海边日落",
  "description": "2025 年元旦在汕头南澳岛拍摄",
  "tags": ["风景", "旅行", "日落"],
  "takenAt": "2025-01-01T17:20:00+08:00",
  "uploadedAt": "2025-02-03T10:00:00Z",
  "size": 2451000,
  "width": 4032,
  "height": 3024,
  "mime": "image/jpeg",
  "orientation": 1,
  "dominantColor": "#7a9bbf",
  "exif": { "camera": "iPhone 15", "iso": 64, "focal": 4.2, "gps": null },
  "origKey": "originals/202502/k9f2x1.jpg",
  "thumbKey": "thumbs/202502/k9f2x1.webp"
}
```

### 4.3 为什么不用数据库

- 个人图库量级小，`list({ prefix: 'meta/…' })` 一次可拉全部元数据（每条 ~500B，1 万张约 5MB，可缓存到浏览器 IndexedDB）；
- 搜索 / 过滤在**客户端**做（全文模糊匹配标题、标签、描述），响应快、零查询成本；
- 省掉数据库 = 少一类故障与账单；若未来超过 2~3 万张再引入 SQLite/LibSQL 也不迟（meta JSON 可原样导入）。

---

## 5. 功能与页面规划（v0.6 起为单页应用，v0.8 起为多窗口）

| 视图 | 功能 | 载体 |
|---|---|---|
| 图库视图 | **极简瀑布流**：瀑布流网格（真实图片）+ 无限滚动 + 灯箱预览（含信息面板）；标签筛选收于**悬浮弹出菜单**（标签按钮触发） | `html/index.html` 主壳 |
| 搜索窗口 | 大搜索框 + 方形结果网格 + 计数/空态，即时过滤，点击结果开灯箱；可参与窗口层叠 | index 内 `.page-panel#panelSearch` |
| 上传窗口 | 拖拽/点选多文件、客户端压缩、标签填写、上传进度；**悬浮窗口**，可与其他窗口层叠 | index 内 `.page-panel#panelUpload` |
| 设置窗口 | 站点信息、存储用量、标签管理、数据导出/清空；**悬浮窗口**，可层叠 | index 内 `.page-panel#panelSettings` |
| 详情展示 | 由**灯箱信息面板**承担（标题、日期、大小、分辨率、格式、标签、前后切换） | index 内 `.lightbox` |

> 导航规范（v0.8.6）：无顶部导航栏。右下角**悬浮按钮组**（主按钮悬停展开子按钮）——「页面切换」悬停弹出菜单（图库 / 搜索 / 上传 / 设置），「标签筛选」悬停弹出**主要标签菜单**（Top 8）；上传/设置/**搜索**均以**悬浮窗口**打开（Genie 喷出），多窗口**旋转式文件堆叠**、点击置顶；`upload.html / settings.html / detail.html` 保留为遗留文件（不再作为导航入口）。

> 视觉规范（v0.2，v0.3–v0.8.6 沿用）：全站统一「**黑色毛玻璃**」风格（v0.8.5 起）—— 深色半透明毛玻璃层（`backdrop-filter: blur(40px) saturate(160%)`）+ 白色半透明描边/内高光 + 背景动态光斑；动效遵循：弹性曲线（`--ease-spring` / `--ease-out`）、入场 `rise`/`card-in`、悬停上浮、按压缩放、窗口 **Genie 吸入/喷出**（SVG 波浪滤镜 + 弹性过冲，v0.7）、**旋转式文件堆层叠**（v0.8.1）、**悬浮菜单弹性长出 + stagger 浮现**（v0.8.3/0.8.4）、灯箱 `lb-zoom` 与信息面板 `slide-up`、按钮组呼吸光晕与旋转展开；`prefers-reduced-motion` 时全部降级为瞬时。

---

## 6. API 设计（Netlify Functions）

统一前缀 `/api`，全部要求 `X-Auth-Token`（比对环境变量 `ADMIN_TOKEN`）。

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/photos?cursor=&tag=&q=` | 元数据分页列表（游标分页，倒序）；`tag` 参数 v0.4 起随侧边栏筛选重新启用 |
| GET | `/api/photos/:id` | 单张元数据 |
| GET | `/api/photos/:id/raw?dl=1` | 图片字节输出；`?w=&q=` 时走 Image CDN 优化；`dl=1` 触发下载 |
| POST | `/api/photos` | 上传：multipart（原图 + 缩略图 + meta JSON 一包提交） |
| PATCH | `/api/photos/:id` | 更新标题/描述/标签 |
| DELETE | `/api/photos/:id` | 删除原图/缩略图/元数据 + 清理标签索引 |
| GET | `/api/meta/stats` | 用量统计（张数、总字节、按月份分布） |
| GET | `/api/export` | 导出全部元数据 JSON（迁移用） |

**响应约定**：列表/详情返回 JSON；图片返回字节 + `Content-Type` + `Cache-Control: public, max-age=31536000, immutable`（key 带 id，天然不可变）；所有响应带 `X-Content-Type-Options: nosniff`。

---

## 7. 关键流程设计

### 7.1 上传（重点：绕开请求体与响应体积限制）

```
选择图片 → 浏览器端 canvas 处理：
  1. 剥离无用 EXIF、纠正 orientation
  2. 原图重压缩：质量 0.82，最长边 ≤ 4096，体积 ≤ 8MB（超过则继续降质）
  3. 生成缩略图：最长边 ≤ 800 的 WebP（质量 0.75）
  4. 采样主色调 dominantColor（占位底色）
→ multipart 一包提交：原图 + 缩略图 + meta.json
→ Function 校验（类型白名单 / 大小 / 图片魔数）→ 写 3 个 key + 更新标签索引
→ 返回 { id }
```

- 不做服务端转码（省算力、免超时），缩略图由前端生成；
- 单请求 < 10MB，绕开请求体限制；
- 上传中显示逐文件进度（XHR `upload.onprogress`）。

### 7.2 浏览 / 搜索

- 首次进入：`GET /api/photos` 拉第一页（每页 60 条 meta）→ 渲染网格，滚动到底部游标翻页；
- 标签筛选（v0.4，v0.6 沿用）：右侧侧边栏列出全部标签（含计数），选中后与搜索关键词叠加过滤；`tag` 参数重新启用；
- 搜索：搜索框输入即时在客户端过滤（匹配标题 / 描述 / 标签）；命中不足时提示"换个关键词试试"；
- 详情展示（v0.6）：点击卡片打开灯箱，大图 + 右侧信息面板（标题、日期、大小、分辨率、格式、标签），`←` `→` 键前后切换；
- 图片地址：`/.netlify/images?url=/api/photos/<id>/raw&w=600&fm=webp&q=75` —— 缩略展示统一走 Image CDN，浏览器缓存友好；灯箱大图 `w=2048`；原图走独立按钮（Function 流式输出）。

### 7.3 图片输出（绕开 10MB 响应限制）

| 场景 | 方案 |
|---|---|
| 缩略图 / 详情大图 | Image CDN 裁剪输出，单张 ≤ 2MB，完全无压力 |
| 原图下载 | `dl=1` 时 Function 流式输出；超限风险图（>10MB）给出提示或分块下载，或限制上传时最长边 ≤ 4096 从源头规避 |

### 7.4 删除

删除 `originals / thumbs / meta` 三个 key → 遍历 `index/tags/*` 移除该 id → 空索引 key 一并删除。设置页的"清空图库"为逐批 `list + delete` 的批量任务。

---

## 8. 安全设计

- Blobs 默认私有；无 Token 的请求一律 401；
- `id` 不可枚举 + URL 带 CDN 签名参数，防止遍历抓图；
- 上传校验：MIME 白名单（jpeg/png/webp/gif/avif）、魔数嗅探、大小上限、图片重采样炸弹防护（解码前检查尺寸）；
- 输出安全头：`nosniff`、`Content-Disposition`（下载场景）；
- Token 存于环境变量，前端不落任何密钥；可选：上传接口二次 Token（`UPLOAD_TOKEN`）与浏览分离。

---

## 9. 性能与限额对策

| 限制 | 对策 |
|---|---|
| Functions 响应体 ~10MB | Image CDN 输出优化图；上传源头控制文件大小 |
| 存储配额（套餐制） | 设置页展示用量；上传前预估剩余空间；提供"压缩入库"选项 |
| 单次 list 返回上限 | 游标分页，每页 60 条 |
| 冷启动延迟 | Functions 保持最小依赖（仅 `@netlify/blobs`）；图片走 CDN 缓存一年，列表缓存 60s |
| 并发上传 | 前端队列化，最多 3 并发，失败自动重试 1 次 |

---

## 10. 部署方案

```toml
# netlify.toml
[build]
  command = "npm run build"      # 前端构建
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
```

- 新建站点 → 连接 Git 仓库（或 `netlify deploy` 直传）；
- 在站点后台设置环境变量：`ADMIN_TOKEN`、`UPLOAD_TOKEN`；
- Blobs 随站点自动创建，无需额外开通。

**建议的项目目录结构**

```
gallery/
├── netlify/
│   └── functions/
│       ├── photos-list.ts
│       ├── photo-get.ts
│       ├── photo-raw.ts
│       ├── photo-upload.ts
│       ├── photo-patch.ts
│       ├── photo-delete.ts
│       ├── stats.ts
│       └── export.ts
├── src/            # 前端（SPA 主壳 index + 面板视图，见 §13）
├── public/
├── netlify.toml
└── package.json
```

---

## 11. 里程碑

| 阶段 | 内容 | 验收 |
|---|---|---|
| M1 | 骨架：静态页 + 列表/详情/图片输出 API | 能浏览预置图片 |
| M2 | 上传链路：压缩 + multipart + 三 key 写入 | 能上传并立即看到 |
| M3 | 搜索 / 标签 + 编辑信息 | 常规管理闭环 |
| M4 | 设置页：用量、导出、清空；安全加固 | 可安心长期使用 |
| M5 | 打磨：动效细节、键盘导航、响应式、加载占位 | 体验流畅 |

---

## 12. 风险与备选方案

| 风险 | 影响 | 备选 / 缓解 |
|---|---|---|
| 配额用尽 | 无法上传 | 设置页预警；一键"导出 + 清空"；升级套餐 |
| 无数据库导致检索弱 | 大量图片后变慢 | 客户端 IndexedDB 缓存；迁移 SQLite |
| 图片字节输出受限 | 原图无法完整下载 | 限制入库最长边；分块下载 |
| Netlify 平台变更 | 迁移成本 | meta JSON 可导出，Blobs 支持 list+get 全量备份，可脚本迁往 S3/R2 |

---

## 13. 附：设计稿说明

`html/` 目录为**静态设计稿**（无后端），整体为**苹果液态玻璃**风格（v0.2 起，v0.6 深化）：

- `index.html` — **单页应用主壳（v0.6）**：图库瀑布流（真实图片）+ **上传/设置/搜索悬浮窗口**（v0.8/0.8.6，旋转式文件堆叠）+ 灯箱（含信息面板）+ 悬浮按钮组（页面切换菜单 / 标签菜单）
- `image/` — 243 张真实图片素材（webp / jpg / png），由 `app.js` 的 `IMAGES` 数组引用
- `upload.html` / `detail.html` / `settings.html` — **遗留独立页面**（v0.6 起不再作为导航入口，功能已并入 index 面板 / 灯箱）
- `assets/style.css` — 液态玻璃主题（弹性曲线、折射毛玻璃、光斑背景、全部动效）
- `assets/app.js` — 演示数据 + 交互（SPA 切换、筛选、灯箱、上传模拟）

交互细节（v0.8.6）：右下角悬浮按钮组——主按钮悬停展开两个子按钮（筛选 / 页面切换）；「页面切换」**悬停即弹出**液态玻璃菜单（图库 / 搜索 / 上传 / 设置，菜单项逐个浮现，点击固定）；「标签筛选」悬停弹出**主要标签菜单**（Top 8 + 全部行，stagger 弹出），选中后菜单收起、按钮亮橙色角标；**搜索为独立窗口**（菜单「搜索」或 `/` 快捷键打开）：大输入框即时过滤，方形结果网格点击开灯箱；上传 / 设置 / 搜索窗口均以 Genie 喷出打开、旋转式文件堆叠、点击置顶、交通灯红点或返回按钮关闭（吸入）、菜单「图库」关闭全部；灯箱含信息面板，`←` `→` 键切换、Esc 关闭；菜单均支持点击固定、外部或 Esc 收起。

真实环境将 `IMAGES` 数据源替换为 `/.netlify/images?url=/api/photos/<id>/raw&w=…` 即可。
