# 留影 Gallery · 完整功能规划文档

> 本文档用于指导 AI 编码助手（DeepSeek V4 Flash）逐步实现图库功能。
> 每个功能包含：需求描述、数据结构、实现要点、涉及文件。

---

## 项目概览

- **项目名**：留影 Gallery（私人图库）
- **技术栈**：纯前端 HTML/CSS/JS + Netlify Functions（Node.js）+ Netlify Blobs（存储）
- **无构建工具**：直接引用 `public/assets/app.js` 和 `public/assets/style.css`
- **本地开发**：`npm run dev` 启动 `scripts/dev-server.js`，内存模拟 Blobs，端口 8787
- **部署**：Netlify 自动部署，Functions 目录 `netlify/functions/`

### 文件结构

```
public/
  index.html          ← 单页应用，所有 HTML 结构
  assets/
    app.js            ← 所有前端逻辑（约 900 行）
    style.css         ← 所有样式（约 2000 行）
netlify/
  functions/
    photos.js         ← API 处理（增删改查图片）
    _lib.js           ← 共享工具函数
```

### 现有 API 路由（photos.js）

- `GET /api/photos` — 获取图片列表（支持 ?limit=&cursor= 分页）
- `POST /api/photos` — 上传图片（multipart/form-data）
- `DELETE /api/photos` — 清空图库
- `GET /api/photos/:id/raw` — 获取原图
- `GET /api/meta/stats` — 获取统计信息（张数、字节数）
- `GET /api/export` — 导出元数据 JSON

### 现有前端关键变量（app.js）

- `PHOTOS` — 图片数组，每项 `{ id, title, tags[], size, width, height, mime, takenAt, url }`
- `USE_API` — 是否使用真实 API（本地开发为 true）
- `apiHeaders()` — 返回请求头（含 token）

---

## 功能一：设置页扩展

### 需求

在设置面板中增加更多配置项。当前设置页是 Apple iOS 风格的分组列表（`settings-section` + `settings-group` + `settings-row`）。

### 新增设置项

#### 1.1 外观设置

```
settings-section: "外观"
├── 主题切换（浅色/深色/跟随系统）
│   └── 实现：CSS 变量 + data-theme 属性 + transition 过渡
├── 瀑布流列宽（窄 180px / 标准 240px / 宽 320px）
│   └── 实现：修改 .masonry 的 columns 属性
└── 语言（中文/English）— 可选，优先级低
```

**主题切换实现要点：**
- `:root` 和 `[data-theme="light"]` 定义两套 CSS 变量
- `body` 上加 `transition: background-color 0.4s, color 0.4s`
- `localStorage` 存储用户偏好
- `@media (prefers-reduced-motion: reduce)` 时禁用过渡

#### 1.2 AI 助手设置

```
settings-section: "AI 助手"
├── 开关（启用/禁用）
│   └── localStorage 存储，禁用时隐藏所有 AI 相关入口
├── API Key（质谱 AI BigModel）
│   └── type="password"，localStorage 存储，显示/隐藏切换
├── 人设（System Prompt）
│   └── textarea，默认值："你是一个可爱的猫娘图库助手，说话带喵~"
├── 温度（Temperature）
│   └── range 滑块，0.0-1.0，默认 0.7
└── 测试对话按钮
    └── 发送 "你好" 验证 API Key 是否有效
```

**质谱 AI 调用方式：**
- API 地址：`https://open.bigmodel.cn/api/paas/v4/chat/completions`
- 模型：`glm-4-flash`（免费）
- 请求格式：OpenAI 兼容
- 通过 Netlify Function 代理调用（避免前端暴露 Key）

---

## 功能二：标签体系重构

### 需求

当前标签是简单字符串数组。需要重构为支持分组、别名、搜索的完整体系。

### 数据结构

**Netlify Blobs 中存储标签配置（key: `tags-config`）：**

```json
{
  "groups": [
    {
      "id": "game-char",
      "name": "游戏角色",
      "color": "#ff9f0a",
      "sort": 0
    },
    {
      "id": "scene",
      "name": "场景",
      "color": "#58a6ff",
      "sort": 1
    }
  ],
  "tags": [
    {
      "id": "hutao",
      "name": "胡桃",
      "group": "game-char",
      "aliases": ["核桃", "Hu Tao", "hu tao"],
      "color": null
    },
    {
      "id": "ganyu",
      "name": "甘雨",
      "group": "game-char",
      "aliases": ["椰羊"]
    }
  ]
}
```

**图片元数据中的 tags 字段改为 tag ID 数组：**
```json
{
  "id": "photo-xxx",
  "tags": ["hutao", "ganyu", "inazuma"]
}
```

### API 新增路由

- `GET /api/tags` — 获取标签配置（groups + tags）
- `PUT /api/tags` — 更新标签配置（整体替换）
- `POST /api/tags` — 新增标签
- `DELETE /api/tags/:id` — 删除标签

### 前端实现要点

- 标签筛选菜单：显示标签组 → 点击展开该组下标签
- 标签搜索：输入框支持模糊匹配标签名和别名
- 上传时打标签：下拉选择或搜索，显示所属组
- 标签管理（设置页）：可增删改标签组和标签，支持拖拽排序

---

## 功能三：AI 标签助手

### 需求

利用质谱 AI（GLM-4-Flash 免费模型）辅助标签管理，不直接分析图片内容。

### 功能点

#### 3.1 智能标签搜索
- 用户在标签筛选输入框输入自然语言（如「原神角色」）
- 调用 AI 返回相关标签 ID 列表
- 前端按返回的标签过滤图片

#### 3.2 自然语言筛选
- 搜索框支持自然语言（如「找所有稻妻相关的图」）
- AI 解析为标签组合 `{ tags: ["inazuma", "kazuha", ...], match: "any" }`
- 前端执行筛选

#### 3.3 上传时标签建议
- 用户输入图片描述或文件名
- AI 根据现有标签库推荐应打的标签
- 用户确认后应用

#### 3.4 标签整理建议
- 扫描所有标签，AI 建议：
  - 合并相似标签（「核桃」→「胡桃」）
  - 归类未分组标签
  - 删除无引用标签

### Netlify Function：ai-proxy.js

```javascript
// netlify/functions/ai-proxy.js
// 路由：POST /api/ai/chat
// 请求体：{ messages: [...], temperature: 0.7 }
// 从环境变量或请求头获取 API Key
// 代理转发到质谱 API

export default async (req) => {
  const { messages, temperature } = await req.json();
  const apiKey = req.headers.get("x-ai-key") || process.env.ZHIPU_API_KEY;
  
  const res = await fetch("https://open.bigmodel.cn/api/paas/v4/chat/completions", {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      "Authorization": `Bearer ${apiKey}`
    },
    body: JSON.stringify({
      model: "glm-4-flash",
      messages: [
        { role: "system", content: "你是一个图库标签助手..." },
        ...messages
      ],
      temperature: temperature || 0.7
    })
  });
  
  const data = await res.json();
  return new Response(JSON.stringify(data), {
    headers: { "Content-Type": "application/json" }
  });
};
```

### 前端调用示例

```javascript
async function aiTagSearch(query) {
  const tagsConfig = await fetch("/api/tags").then(r => r.json());
  const tagList = tagsConfig.tags.map(t => `${t.name}(${t.aliases.join(",")})`).join("、");
  
  const res = await fetch("/api/ai/chat", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({
      messages: [
        { role: "system", content: `你是标签助手。现有标签：${tagList}。用户查询时，返回匹配的标签ID数组，格式：["id1","id2"]` },
        { role: "user", content: query }
      ]
    })
  });
  const data = await res.json();
  return JSON.parse(data.choices[0].message.content); // ["hutao", "ganyu"]
}
```

---

## 功能四：图片浏览增强

### 4.1 图片信息浮层

- 灯箱（lightbox）右上角加 `i` 按钮
- 点击后从右侧滑出信息面板（半透明毛玻璃）
- 内容：尺寸、格式、文件大小、拍摄时间
- EXIF 信息可选展示（需要解析 EXIF，优先级低）
- 默认隐藏，不打扰浏览

### 4.2 收藏功能

- 存储：`localStorage.setItem("favorites", JSON.stringify(["id1", "id2"]))`
- 灯箱和卡片上加星标按钮
- 标签筛选菜单加「收藏」快捷筛选
- 无需登录，单设备生效

### 4.3 图片下载

- 灯箱加下载按钮
- 使用 `<a download>` 触发下载
- 批量下载：选中多张后打包 ZIP（需要 JSZip 库，可选）

### 4.4 幻灯片放映

- 灯箱加播放按钮
- 自动切换下一张（间隔 3/5/10 秒可调）
- 全屏模式 + 自动隐藏控件
- 按空格暂停/继续

---

## 功能五：上传增强

### 5.1 上传前预览

- 拖拽/选择文件后显示预览队列
- 每张可编辑：重命名、添加描述、选择标签
- 支持删除队列中的单张

### 5.2 URL 导入

- 输入框粘贴图片 URL
- Netlify Function 下载图片并存入 Blobs
- 路由：`POST /api/photos/import` `{ url: "https://..." }`

### 5.3 重复检测

- 上传前计算文件 hash（MD5/SHA-256）
- 与已有图片比对，提示重复
- 可选跳过或强制上传

---

## 功能六：存储与性能

### 6.1 存储用量进度条

- 设置页图库统计区域加进度条
- 假设总配额 1GB（可配置）
- 进度条颜色：<70% 正常色，70-90% 橙色，>90% 红色
- 显示：已用 / 总量

### 6.2 缩略图

- 上传时自动生成缩略图（Netlify Function 中处理）
- 缩略图存为单独的 Blob key：`thumb-{id}`
- 列表页加载缩略图，灯箱加载原图
- 需要 sharp 或 jimp 库（在 Function 中使用）

---

## 样式规范

- 主色调：`--accent: #ff9f0a`（橙色）
- 背景：深色 `--bg: #08090d`
- 毛玻璃：`backdrop-filter: blur(40px) saturate(160%)`
- 圆角：`--radius: 18px`（大卡片），`--radius-sm: 12px`（小元素）
- 字体：SF Pro / PingFang SC / Microsoft YaHei
- 动画：`cubic-bezier(0.16, 1, 0.3, 1)` 缓出
- Apple 风格：分组列表、交通灯按钮、液态玻璃效果

---

## 开发顺序建议

1. **标签体系重构**（基础，其他功能依赖）
2. **设置页扩展**（主题 + AI 设置）
3. **AI 标签助手**（依赖标签体系 + 设置页）
4. **收藏功能**（独立，可随时做）
5. **图片信息浮层**（独立）
6. **上传增强**（依赖标签体系）
7. **存储可视化**（独立）
8. **幻灯片/下载**（独立）

---

## 注意事项

- 所有新增 API 路由写在 `netlify/functions/photos.js` 中（或新建文件）
- 前端所有逻辑在 `app.js` 中，样式在 `style.css` 中
- 不引入构建工具，不使用 npm 前端框架
- 可以引入 CDN 库（如 JSZip、sharp 等）
- localStorage 用于用户偏好存储（主题、收藏、AI 配置）
- Netlify Blobs 用于服务端数据（图片、元数据、标签配置）
- 环境变量：`ZHIPU_API_KEY`（质谱 AI Key，可选，用户也可在设置页输入）

---

## 已完成 ✅

- [x] 设置页 Apple 风格重写（settings-section + settings-group + settings-row 分组列表）
- [x] 移除管理员登录，所有功能直接可用
- [x] 图库统计（图片数量、已用空间）
- [x] 标签管理（设置页展示已有标签，支持删除）
- [x] 数据维护 - 导出元数据 JSON
- [x] 危险操作 - 清空整个图库（带确认弹窗）
- [x] 标签筛选菜单（悬浮弹出，按标签过滤图片）
- [x] 标签搜索（输入框模糊匹配）
- [x] 搜索窗口（独立界面，搜索标题/标签/描述）
- [x] 上传功能（拖拽 + 点击选择，支持多张，标签输入）
- [x] 灯箱浏览（大图查看，左右切换，键盘快捷键）
- [x] 瀑布流布局（自适应列数，列宽 240px）
- [x] 翻转卡片悬停效果（hover 从底部翻转显示标题/标签/日期）
- [x] 仓鼠跑轮加载动画
- [x] FAB 悬浮按钮组（主按钮 + 标签筛选 + 页面切换）
- [x] 页面切换菜单（图库/搜索/上传/设置）
- [x] 悬浮窗口系统（上传/设置/搜索独立窗口，可层叠，Genie 动画）
- [x] 无限滚动加载
- [x] 视口出现动画（卡片交错淡入）
- [x] **标签体系 v0.11**（2026-09-02，校准后补录）：
  - 标签组 / 别名 / 颜色：配置存 Blobs key `tags-config`（`{ groups:[{id,name,color,sort}], tags:[{id,name,aliases,group,color,sort}] }`）
  - API：`GET /api/tags`、`PUT /api/tags`、`POST /api/tags/rename`（改名/合并并同步照片引用）、`POST /api/tags/remove`（删除并同步照片）
  - 照片 `meta.tags` 仍存标签名（名称即引用键，旧数据零迁移）
  - 筛选菜单按组分开展示 + 折叠 + 搜索（匹配名称与别名）
  - 卡片 / 灯箱 / 搜索结果标签 chip 按组着色
  - 上传输入框实时标签库建议（显示所属组）
  - 设置页标签管理：组与标签增删改、别名、色板、改名同步照片
  - 清空图库时保留 `tags-config`；顺带修复页面菜单点击 ReferenceError 回归

---

## 实施批次计划（2026-09-02 校准版）

> 依据**当前实际代码**（v0.9.24 登录体系 + 标签体系 v0.11）规划。
> 每批独立可验收，做完一批汇报一批；本表同步维护状态。

### 批次 1 · 设置页 · 外观与快捷键（纯前端）✅ 已完成（2026-09-02）
- 主题切换：浅色 / 深色 / 跟随系统
  - CSS 变量两套（`:root` 深色 + `[data-theme="light"]`），`<html data-theme>`，localStorage 记忆，body 过渡 0.4s，`prefers-reduced-motion` 时禁用
- 瀑布流列宽：窄 180px / 标准 240px / 宽 320px（localStorage，改 `.masonry` column 宽度）
- 快捷键说明：设置页「快捷键」分组列出 `/`（搜索）、`Esc`（关闭）、`←→`（灯箱切换）等
- 图片加载质量：并入批次 3（依赖服务端缩略图）
- 语言切换：推迟到最后一批（等全部 UI 定型后一次翻译，避免每批双份文案）
- 涉及文件：`index.html`（设置页外观分组）、`app.js`、`style.css`

### 批次 2 · 图片浏览与管理 ✅ 已实施（2026-09-02，待验收）
- 灯箱信息面板：标题 / 描述 / 尺寸 / 格式 / 大小 / 时间 / 标签；`i` 按钮可收起（`no-info`），看图区自动占满
- 收藏：卡片悬停星标 + 灯箱星标，localStorage `rn_favs`；筛选菜单「收藏」行快捷筛选
- 图片下载：灯箱下载按钮（原图，标题命名）
- 图片旋转：灯箱旋转按钮 = 顺时针 90° 保存（前端 canvas 重编码 → `POST /api/photos/:id/image` 覆盖原图，服务端更新尺寸/大小/时间；绕过 immutable 缓存刷新展示）
- 编辑弹窗：标题 / 描述 / 标签（标签库建议输入），`PATCH /api/photos/:id`；弹窗内可删除当前图片（确认）
- 批量操作：FAB「选择」进入多选模式 → 卡片勾选 → 底部操作条（删除 / 加标签），Esc 或取消退出；打开窗口自动退出
- 图片排序：FAB「排序」菜单：最新上传 / 最早上传 / 标题 A–Z / 文件大小（localStorage `rn_sort`，与筛选叠加）
- 灯箱 ←→ 切换按当前筛选排序视图顺序；搜索打开灯箱时回退全图顺序
- 后端新增：`POST /api/photos/:id/image`（替换原图字节并更新元数据）

### 批次 3 · 存储与上传 ✅ 已实施（2026-09-02，待验收）
- 存储用量进度条：统计区配额进度条（默认 1GB，`QUOTA_BYTES` 可配），<70% 绿 / 70-90% 橙 / >90% 红
- 缩略图：**前端生成** 480px 缩略图随上传/旋转一并保存（零原生依赖，webp 兼容），新路由 `GET /api/photos/:id/thumb`；图片加载质量三档：高画质=原图 / 平衡=列表缩略图 / 省流=灯箱也用缩略图；老图与导入图自动回退原图
- URL 导入：上传页链接列表 → `POST /api/import`（逐条反馈成功/失败）
- 重复检测：内容 sha1 存 meta，`GET /api/photos/check?hash=` 预检，重复时确认「跳过 / 仍然上传」

### 批次 4 · AI 助手（质谱 GLM-4-Flash）✅ 已实施（2026-09-02，待验收）
- ai-proxy.js（POST /api/ai/chat，Key 取 X-AI-Key 头或 ZHIPU_API_KEY）已联调真实 Key 通过；智能标签搜索 / 自然语言筛选 prompt 已实测；整理建议=本地规则（别名冲突合并、无引用删除）+ AI 补充并做合法性过滤
- 设置页「AI 助手」分组：开关 / API Key（password，localStorage）/ 人设 / 温度 / 测试对话
- 新 Function `netlify/functions/ai-proxy.js`：`POST /api/ai/chat` 代理转发（key 取请求头或环境变量 `ZHIPU_API_KEY`），config.path 注册
- 功能：智能标签搜索（自然语言 → 标签列表）、自然语言筛选（→ 标签组合）、上传时标签建议、标签整理建议（合并 / 归类 / 删无引用）
- 无 Key 时入口自动隐藏
- 涉及文件：`ai-proxy.js`、`photos.js`（config.path）、`index.html`、`app.js`、`style.css`

### 批次 5 · 相册 / 安全 / 语言（收尾）
- 相册：`albums-config` Blob（相册列表 + 照片 id 集合），图库页相册视图与切换、相册管理（新建/重命名/删除/加入移除照片）
- 访客密码：全站访问密码 gate（`VIEW_TOKEN` 环境变量；登录弹窗区分「访客 / 管理员」，与现有 ADMIN_TOKEN 体系共存）
- 操作日志：写操作记录（时间 / 动作 / 对象 / IP）存 Blob `logs`，设置页查看与清空
- 图片水印：上传时可选烧入（右侧 PNG 水印文件上传到设置页，jimp 合成到缩略图/原图）
- 语言切换 zh/en：全部 UI 文案收尾翻译（`lang` 字典 + localStorage）
- 涉及文件：全局

### 不在当前范围（明确不做）
- EXIF 解析展示（灯箱信息面板仅展示现有字段）
- 拖拽排序标签（管理页不做 DnD，以 sort 顺序为准）
- 音乐 / 视频等非图片文件支持

---

### 批次 6 · 收尾补全（v0.13，2026-09-02）✅
- 幻灯片放映：灯箱播放/暂停按钮、间隔 3/5/10 秒（设置页）、空格键播放暂停、播放中控件自动隐藏、关闭/Esc/换图自动停止
- 标签拼音搜索：pinyin-pro（CDN，离线自动降级）支持全拼/首字母匹配标签名与别名（菜单、上传与编辑建议、搜索窗口）
- 上传前逐张编辑：队列 hover 出现 ✎（标题/描述/标签，留空回退全局）与 ✕ 移除；自动上传延迟 3 秒留出编辑时间
- 语言切换完善：动态文案双语（筛选菜单行 / 批量条 / 排序 / 搜索计数 / 加载提示），骨架词典扩充（弹窗按钮、上传、水印、访客门禁等）

### 批次 7 · 主分类体系（v0.15，2026-09-09）✅
- 上传必选「主分类」：独立 `meta.category` 单值字段（不进 tags 数组），单选互斥；内置默认 次元女/次元男/插画/风景/美女/帅哥（tags-config 新增 `categories` 数组，可增删改、自定义颜色）
- 上传面板三段引导：顶部「主分类（必选）」chips → 未选不开始上传（校验 + 闪烁提示，选后自动恢复 15s 倒计时）；每张可在 ✎ 内单张覆盖（留空跟随全局），队列行显示最终主分类 chip
- 编辑图片弹窗 / 批量加标签弹窗均可补主分类；老图无主分类显示「未分类」，可在筛选菜单/编辑中补齐
- 筛选菜单：主分类区（分类名 + 未分类行，与标签/收藏/AI 筛选叠加 AND）；卡片、灯箱、搜索结果展示主分类 chip
- 标签管理：分组视图顶部主分类管理块（计数 + 改名/删除/新建，走色板）；改名/删除同步全部照片的 category（新增 `POST /api/tags/category-rename`、`category-remove`；PUT /api/tags 删除分类也同步置空照片）
- 涉及文件：`photos.js`（结构 + 3 端点 + 同步遍历）、`app.js`（上传/编辑/筛选/管理）、`index.html`（4 处面板）、`style.css`（chips/管理块样式）
- 语义建议：标签组 = 作品/来源（如 原神、绝区零），组内标签 = 角色小标签 → 原「游戏角色」组可改为按作品分组建组
### 批次 8 · 瀑布流图片防重排（2026-09-13）✅
- 现象：卡片图片是 loading="lazy"，加载完成前卡片矮、完成后被图片撑高；而 `.masonry` 是 CSS 多列布局（columns），任一卡片高度变化都会让整列重新分配，于是滚动时卡片不停位移
- 修复：服务端本就记录了照片宽高（p.width / p.height），`cardHTML()` 渲染时直接写 `style="aspect-ratio:w / h"` 作为占位；`.card img` 由 `height:auto` 改为 `height:100%` + `object-fit:cover`，图片填满卡片而不再撑开容器 → 图片加载前后卡片高度完全一致，瀑布流不再重排（首次渲染与「加载更多」追加的卡片都受益）
- 兼容：老数据缺少宽高时不写 aspect-ratio，退回原来的自然高度行为
- 涉及文件：`public/assets/app.js`（cardHTML）、`public/assets/style.css`（.card img）、`public/index.html`（资源版本号 → ?v=20261021）

### 批次 9 · 访问密码门禁 + R18 分级保护（v0.16，2026-09-13）✅
- 访问密码：除 `/api/auth/state`、`/api/auth/login` 外的所有 API 都要校验 `X-Auth-Token` 或 `?token=`；密码只存 SHA-256 哈希（Blobs 的 `auth-config`），首次启动用环境变量 `ADMIN_TOKEN` 初始化；**未设置密码时自动放行**，不会把自己锁在外面
- 图片是 `<img>` 直接加载、带不了请求头，所以凭证走 URL 参数：列表返回的 `url` / `thumbUrl` 已自动拼接 `?token=`
- 前端门禁页：启动时先请求 `/api/auth/state`，启用密码且本机无凭证时只显示登录框（不加载任何数据），登录成功写入 localStorage 后重载
- R18 分级保护：判定方式三者任一命中——独立字段 `r18 === true`、标签含 `r18`、主分类为 `r18`（后端统一计算，随列表返回 `r18` 标记）
  - 未解锁时：普通筛选（含「全部」）下 R18 图片**根本不进列表**；正在筛选 R18 相关内容时渲染**锁定占位卡**（不加载任何图片字节）
  - 解锁：点锁定卡输入 R18 密钥 → `POST /api/auth/r18/verify` → 通过后本机记住，图片 URL 追加 `?r18Key=`
  - 后端 `raw` / `thumb` 对 R18 图片校验密钥，未通过返回 401（即使已通过访问密码也拿不到字节）
- 设置页新增「访问与保护」：修改访问密码、设置/清除 R18 密钥、退出登录（清除本机凭据）；忘记密码可在 Netlify 改 `ADMIN_TOKEN` 并清空 Blobs 的 `auth-config` 重新初始化
- 新增端点：`GET /api/auth/state`、`POST /api/auth/login`、`POST /api/auth/password`、`POST /api/auth/r18`、`POST /api/auth/r18/verify`
- 涉及文件：`netlify/functions/_lib.js`、`netlify/functions/photos.js`、`public/index.html`、`public/assets/app.js`、`public/assets/style.css`（资源版本号 → ?v=20261022）
- 待办：本地 `scripts/dev-server.js` 尚未实现 `/api/auth/*`，本地开发时门禁不生效、设置页相关按钮会失败（生产环境正常）

### 批次 10 · 标签粒度与录入效率（v0.17，2026-09-13）✅
> 解决「角色太多难选」与「只打作品又搜不到具体角色」的两难：作品级兜底 + 按需细化 + 快速定位。
- **整组筛选（作品级兜底）**：筛选菜单每个组头新增「整组 N」按钮 → 点一下筛出该组任一标签命中的全部图片（含只打了作品标签的图），计数按照片去重；与主分类 / 收藏 / AI 筛选 AND 叠加；组头高亮并自动展开，单标签筛选与整组筛选互斥
- **上传页录入优化**：
  - 分类槽按组折叠（手风琴：一次只展开一个作品组），组头显示组内标签数；未分组与临时自定义槽仍平铺
  - 分类槽搜索框：按名称 / 别名 / 拼音首字母过滤（`ht` → 胡桃），搜索时扁平列出并标注所属组
  - 「最近使用」置顶 10 个标签（localStorage `rn_recent_tags`，上传成功或用过槽标签时记录）
  - 点槽 = 加到选中行（未选则全部待上传），免拖拽；拖动行/整批拖拽行为不变
- **批量导入标签**：标签页「⇪ 批量导入标签」→ 选目标组 + 粘贴名单（每行一个，逗号/竖线分隔别名），一次录入原神/绝区零全部角色，已存在的自动跳过
- 建议策略：作品级兜底（只打作品标签）+ 只在重点图补角色标签 —— 检索靠整组筛选，细化靠角色标签
- 涉及文件：`public/assets/app.js`（整组筛选 / 槽折叠与搜索 / 最近使用 / 批量导入）、`public/index.html`（槽搜索框）、`public/assets/style.css`（组头按钮 / 槽折叠 / 搜索框样式）

### 批次 11 · 角色标签库与导入脚本（v0.18，2026-09-13）✅
- 角色名单数据（`scripts/data/`，均为 UTF-8 JSON：`game` / `chars[{name, aliases}]`，另含 `coverageNote` 说明覆盖范围）：
  - `chars-genshin.json` 原神 102 角色（覆盖到 5.8）
  - `chars-starrail.json` 崩坏：星穹铁道 59 角色（覆盖到 2.7）
  - `chars-zzz.json` 绝区零 53 角色（覆盖到 3.1）
  - `chars-pending.json` **待核对候选** 34 条：原神 6.x 挪德卡莱篇 14、星铁 3.x 翁法罗斯篇 + 4.x 18、绝区零 3.2/3.3 2；带 `confidence` 字段（medium = 有来源标题佐证，low = 仅单条线索/模型记忆）
  - 名单由子代理用 `web_search` 整理；本次环境搜索只返回标题、且沙箱禁网，故最新版本角色只能进 pending，需人工核对
- 新脚本 `scripts/import-chars.js`（Node 20+ 内置 fetch，无新依赖）：
  - 把名单合并进图库标签配置：每个游戏建一个组（带主题色）、每个角色建组内标签（含别名）、并补一个「作品级」同名标签（如「原神」，别名 genshin / 星铁 / 崩铁 / zzz）
  - 幂等：已存在的标签只补别名与归组，可反复运行；**原样回传 `categories`**，不会覆盖自定义主分类
  - 参数：`--url` `--token`（线上门禁密码）`--dry-run`（只预览）`--pending`（含候选）`--only=` `--no-work-tag`；写入前自动备份到 `scripts/data/backup-tags-config-*.json`（已在 .gitignore）
  - 用法：`node scripts/import-chars.js --url=https://<站点> --token=<访问密码>`（本地 dev 直接 `node scripts/import-chars.js`）
- 实测：dry-run / 首次导入（3 组 217 标签，别名如 胡桃→核桃·Hu Tao）/ 复跑幂等（新增 0）/ 主分类保留，全部通过

### 批次 12 · 主分类改为多选 + 历史数据迁移（v0.19，2026-09-16）✅
> 起因：历史数据里 85 张照片同时打了多个分类名（插画+次元女 58、插画+风景 26、三者 1），实际用法是「插画=画风、次元女=人物向」的叠加，与单选互斥的模型不符。
- 数据模型：照片主分类由单值 `meta.category` 改为数组 `meta.categories`（去重、上限 6）；后端全部读写路径兼容旧单值字段（`catsOfMeta()` 归一 + 写入时 `delete meta.category` 自动迁移）
- 后端：上传 / PATCH / URL 导入 / category-rename / category-remove / PUT 删除分类，全部改为数组语义；`_lib.isR18Photo()` 支持「主分类数组含 r18」判定
- 前端：主分类 chips 改为多选（容器 `data-multi="1"`），上传（必选至少一个）、队列行内覆盖（可逐个取消）、图片编辑、批量设置全部支持多选；卡片显示前 2 个 +N 折叠，灯箱显示全部；筛选按「包含任一分类」匹配，未分类 = 数组为空；搜索与主分类计数按数组
- 迁移脚本 `scripts/migrate-photos.js`：把照片标签里的主分类名搬进 `categories`（默认从 tags 移除，避免同一信息重复），并把旧作品名合并到新名（星穹铁道 / 崩坏·星穹铁道 → 崩坏：星穹铁道）；支持 `--dry-run` `--keep-tags` `--no-merge` `--merge "旧=新"` `--r18key`
- 线上执行：迁移前备份 104 张照片元数据与标签配置到 `scripts/data/backup-*.json`（已 gitignore）；迁移 92 张（91 张分类迁移 + 1 张作品名合并）
- 线上最终状态（2026-09-16）：104 张照片 → 91 张有主分类（插画 91 / 次元女 59 / 风景 27），其中 85 张为多分类；未分类 13 张；照片标签合并后为 崩坏：星穹铁道 8 / 原神 5 / 花火 3 / 绯英 2 / 昔涟 1（绯英、昔涟属未导入的候选角色，仍是「未分组」游离标签）
- 注意：Netlify Blobs 写入后读取有约 1~2 分钟传播延迟，校验迁移结果需等待后再读，否则会读到旧快照
- 教训：**先部署后端再跑数据迁移**。首次迁移时线上仍是旧版函数，`categories` 字段被旧代码忽略，导致分类名已从 tags 移除却没写进新字段；已用备份重算恢复，未造成数据丢失

### 批次 13 · 分类界面简化 + 逐张整理 + 去重检测（v0.20，2026-09-16）✅
> 起因：标签管理「分类」视图把 224 个标签全部平铺，用户抱怨「在一堆分类里找一个很麻烦」「不要一次性出现一大串」。
- **分类工作台（标签管理 → 分类）对齐上传面板体验**：右栏新增搜索框（名称 / 别名 / 拼音，`ht` → 胡桃）、组折叠（手风琴，一次只展开一个作品组）、「最近使用」置顶；**点分类槽 = 给已选图片打标**（免拖拽），拖拽方式保留
- **新增「整理」视图**（标签管理 → 整理，第三个 tab）：一次只面对**一张图**，彻底告别长列表
  - 右侧：主分类多选 chips + 标签输入框（即时搜索建议）+ 「最近使用」快捷 chips
  - 键盘流：`Enter` 应用并下一张、`S` 跳过、`1-9` 取最近使用标签、`Backspace` 删最后一个标签、`Esc` 退出
  - 范围筛选：待整理（缺主分类或未打过库内标签）/ 未分类 / 无标签 / 全部，带进度显示
  - 保存后只更新本地数据，不重拉全量；退出时同步刷新筛选菜单计数
- **新增 `scripts/find-duplicates.js`**：下载缩略图 → dHash 感知哈希 → 两两比较汉明距离（只读，不删除）；输出疑似重复组（含建议保留项）与「最相似的 N 对」供人工判断；参数 `--threshold` `--url` `--token` `--r18key` `--json`
- 线上实测（104 张）：内容 hash 重复 0 组、id 重复 0、dHash 近似重复 0 组（阈值 4 / 8 均为 0），最相似一对距离 9（1080×1528 ↔ 1280×2277，属不同构图）→ **判定图库无重复图片**；用户反馈的"重复出现"待按现象定位（详见下轮记录）

### 批次 14 · 上传相似图片提醒 + R18/AI 结论（v0.21，2026-09-16）
- 需求：用户要的不是"事后检测报告"，而是**上传时就提醒**「这张和库里某张很像，要删还是要留」
- 后端（`photos.js`）：
  - 新增 `genDHash(buf)`：sharp 缩放到 9×8 灰度、逐行比较相邻亮度 → 64 位 → 16 位十六进制（与服务端缩略图共用 sharp，零新依赖）
  - 照片 `meta.dhash`：上传 / URL 导入 / 替换原图（旋转保存）时自动计算并存储；列表接口随 meta 一并返回
  - 新增 `POST /api/photos/reindex-dhash`：为历史照片补算 dHash（一次性，返回 updated / skipped / failed）
- 前端（`app.js`）：
  - `dhashOfFile()` 在浏览器本地算 dHash（算法与服务端一致：9×8 拉伸 + 灰度阈值），`hammingHex()` 比距离，阈值 8/64
  - 照片进队列即计算：与**库中**照片比对 + 与**本批其它照片**互相比对，行内显示「⚠️ 相似 N 张」徽标（悬停列出是哪几张、差异多少）
  - 上传每张前再确认一次：「与库中『xxx』相似（差异 n/64），仍要上传吗？取消 = 跳过这张」（可点行右侧 ✕ 从队列移除；跳过状态显示为 ≈）
- 实测：同一张图不同 JPEG 质量（q95 / q55）dHash 距离 **0**（命中）；不同构图距离 **25**（不误报）；`reindex-dhash` 返回 skipped=3
- 线上：部署后跑 `POST /api/photos/reindex-dhash` → **104 张全部补算成功**（updated=104, failed=0）；`scripts/find-duplicates.js` 改为优先复用 `meta.dhash`（与前端同一套表示，含十六进制存储与距离比较），复扫结果与逐张下载一致（最相似一对仍是距离 9）
- **R18 与 AI 的结论**：国内合规视觉大模型（智谱 GLM-4V 等）对成人向内容会直接拒答，**"AI 看图给 R18 图片分类"这条路线不成立**；替代方案待用户选择（本地打标模型 / 仅对非 R18 用 AI / 纯键盘流人工整理）

### 批次 15 · 相似提醒选项扩展 + 本地打标工具（v0.22，2026-09-16）✅
- 相似提醒弹窗升级为多选项（新增 `askChoice()`，复用 confirmModal 容器、用完还原）：
  「用新图替换旧图」（删掉库中最相似那张再上传）/「仍然上传」/「删除这张新图」（直接从队列移除）/「取消」（跳过但保留在队列）
  队列项移除逻辑抽成 `removeUqItem()`，与行内 ✕ 共用
- **新增 `scripts/local-tagger.js`（WD14 Tagger 本地打标）** —— 解决"R18 图 AI 过不了审"：全程本地 ONNX 推理，图片不上传、不经任何内容审核
  - 能力：识别角色（动漫/游戏角色，配合 `chars-*.json` 的英文别名自动映射成中文角色标签）、画风与画面元素、以及分级倾向（general / sensitive / questionable / explicit）
  - 用法：`npm install onnxruntime-node` → `node scripts/local-tagger.js --download`（走 hf-mirror 镜像下模型）→ 先 `--image=某张图` 单张试算 → 再 `--url --token --only-untagged`（默认只报告）→ 确认后加 `--write`；`--write-r18` 可把 questionable/explicit 的照片自动标记 R18；R18 图需 `--r18key`
  - 安全设计：**默认不写回**（必须显式 `--write`）；只写「图库已有且能被映射成中文」的标签，不把英文词典塞进图库；映射表 `scripts/data/tagger-map.json` 可自行编辑
  - 状态说明：本机未安装 onnxruntime-node、也未下载模型，**脚本未在本地实测**；首次使用务必先用 `--image` 单张试算，若标签异常可调 `--threshold` / 检查模型文件

### 批次 16 · 打标手感与性能优化（v0.23，2026-09-16）✅
> 用户反馈：分类操作时无法快速添加、图库图片多时网页卡顿。
- **打标不再全量刷新（最关键）**：原先每打一个标签都 `loadData()` 重拉 200 条元数据并整页重建（15 处调用点里包含打标路径），导致无法连续操作、选中状态与滚动位置丢失。
  现在分类工作台的「点槽打标」与「拖到标签上打标」改为**本地增量更新**：只改内存里的 `p.tags` + 更新该卡片角标 + 刷槽计数，**保留选中项**，可以一路点/一路按不停顿
- **分类工作台键盘流**：选中图片后按 `1-9` 直接打「最近使用」里的标签（与整理视图一致的快捷键），离开视图自动摘掉监听
- **筛选菜单默认折叠**：首次渲染把已有组全部折叠（原先是空 Set = 全部展开，224 行标签一次性进 DOM）；输入搜索词时自动展开命中组
- **整理视图图片加载**：改为缩略图秒开 + 后台加载原图替换 + 预取下一张缩略图（原先每张都等原图下载完）
- **图片解码**：卡片 / 管理页小卡 / 搜索结果统一加 `decoding="async"`
- **新增「性能模式」开关**（设置 → 外观）：关闭 `backdrop-filter` 毛玻璃与大部分过渡/动画（毛玻璃是这类界面最大的 GPU 开销），并停用卡片翻转悬停动画；开关状态存 `rn_perf_lite`，脚本加载时尽早就生效避免首屏闪烁
- 待办（图库规模变大后再做）：`loadData()` 目前固定一次拉 200 张且无游标分页，超过 200 张只能看到最新 200 张 —— 需要改成"首屏 60 张 + 后台静默补齐 + 服务端筛选"，涉及筛选/菜单/相册等多处依赖全量数据的逻辑，建议图库接近 200 张时再做

### 批次 17 · 列表索引（支撑几千张规模，A 批）（v0.24，2026-09-16）✅
> 用户确定图库会到几千张以上。原实现的瓶颈：`list()` 对每张照片各读一次 meta（几千张 = 几千次 Blobs 读），前端还固定一次只拉 200 张。
- **服务端列表索引**（新 Blob `photos-index`）：把列表/筛选/搜索所需的轻量字段（id / title / desc / tags / categories / r18 / 宽高 / 体积 / mime / uploadedAt / takenAt / thumbKey / hash / dhash / src）放进一个数组，按上传时间倒序
  - 写入路径**增量维护**：上传、PATCH 编辑、删除、URL 导入、替换原图、缩略图懒生成 → `indexUpsert` / `indexDrop`
  - 批量改写（标签改名/合并/删除、主分类改名/删除、dhash 补算）→ `indexRebuild` 整体重建
  - 新增 `POST /api/photos/reindex` 可随时重建（修复索引与 meta 不一致）
  - `GET /api/photos` 与 `GET /api/meta/stats` 都改为走索引；分页 cursor 改为**数字偏移**（原为 Blobs 游标字符串），limit 上限 200 → 500；响应新增 `total`
  - 老库首次访问时索引不存在 → 自动构建（慢一次），之后走索引
- **前端加载策略**：首屏只取 **120 张**秒开，剩余**后台静默补齐**（每批 200，逐批刷新筛选菜单计数）；补齐完成后把新数据纳入当前筛选（已显示 ≤300 张时重建列表并保留显示数量，否则只更新计数避免大批 DOM 重建）
- 本地端到端验证（5 张）：上传→索引同步、分页（`limit=2` 三页、total 正确）、PATCH 后立即反映、DELETE 后 total 减少、`reindex` 返回 count、stats 走索引 —— 全部通过
- 已知限制：索引是单个 Blob，到几万张时需要分片；多标签页并发上传可能丢条目（用 `POST /api/photos/reindex` 修复即可）
- 下一批（B）：渲染层优化 —— 只保留可视区卡片、`content-visibility` 跳过屏外渲染、缩略图预取、灯箱渐进加载

### 批次 18 · 渲染层优化（B 批）（v0.25，2026-09-16）✅
- **无限滚动改用 IntersectionObserver 哨兵**：原实现监听 `scroll` 事件并每次读 `document.body.offsetHeight`（强制同步布局，卡片多时滚动掉帧）。现在在网格后插一个 1px 哨兵元素，`rootMargin: 800px` 时触发 `appendMore()`，判断成本几乎为零；追加逻辑抽成 `appendMore()` 供哨兵复用
- **`content-visibility: auto` + `contain-intrinsic-size: auto 320px`**（卡片 / 搜索结果卡片）：浏览器跳过视口外卡片的布局与绘制；用 `auto` 关键字记住上次高度，避免瀑布流重排（若高度异常可直接删这两行，属纯性能优化）
- **性能模式下回收屏外图片**（`initImgRelease`）：把离开视口 1.5 屏以外卡片的图片换成 1×1 占位（DOM 与布局不变），滚回来再恢复 —— 几千张时每张缩略图解码后要几百 KB 内存，这一项省的是内存而非 CPU；仅在「性能模式」开启时生效，风险可控
- **灯箱渐进加载**：先显示缩略图秒开，原图加载完成后自动替换（大图不再白屏等下载）
- `initReveal` 复用**单个** IntersectionObserver（原先每次渲染都新建一个）
- 图库超过 800 张时，「已加载」提示会附带"用筛选或搜索更快定位"的建议
- **新增前端冒烟测试** `scripts/dom-smoke-test.js`：用 jsdom 真跑 `index.html` + `app.js`（stub 掉 IntersectionObserver / fetch 转发到本地服务），验证首屏渲染、哨兵追加、图片回收的离开/恢复、CSS 规则在位 —— 本次 8 项全部通过；用法见文件头（需要 `npm install --no-save --no-package-lock --cache .npm-cache jsdom`）
- `package.json` 增加便捷脚本：`npm run test:dom` / `import:chars` / `find:dups` / `migrate` / `tagger`
- 顺手修掉一个真实缺陷：**初始化不幂等** —— 若 `initGallery()` 被调用两次（jsdom 测试里由"自然派发 + 手动派发 DOMContentLoaded"复现），会插两个滚动哨兵、留两个观察器，导致追加逻辑各自持有一份 `shown/filtered` 状态而失灵。现在创建哨兵前先移除同 id 的旧元素
- 冒烟测试脚本同时修正了 DOMContentLoaded 时序（先等 jsdom 自然派发，没有再补发），失败时会打印内部状态 `{shown, filtered, total, busy}` 与观察器列表，便于定位
- 验证：本地（20 张测试数据）8/8 通过；**线上（真实 104 张、带门禁凭证）8/8 通过** —— 首屏 12 张 → `__appendMore` 后 24 张 → 哨兵触发后 36 张，图片离开视口换占位 / 回来恢复均正常

### 批次 19 · 分类工作台支持主分类（v0.26，2026-09-16）✅
> 用户反馈：「分类部分不支持进行主分类的分类」—— 分类工作台（标签管理 → 分类）原先只能给图片打标签，改主分类只能去编辑弹窗 / 批量弹窗 / 整理视图。
- 分类工作台右栏拆成两块：**主分类面板**（顶部，6 个分类 chips）+ 标签面板（原分类槽，标题改为「标签（作品 / 角色）」）
- 交互与打标一致，且支持多选批量：
  - 点选左侧图片（可多选）→ **点主分类名 = 给选中的图片设置该主分类**；若选中图片**已全部包含**该分类，再点 = 取消（标准 toggle 语义）
  - 面板会高亮"选中图片共有的主分类"（跟随选中项重建）
  - **也支持拖拽**：把图片（含从图库墙拖来的卡片）拖到主分类名上 = 加上该主分类
- 卡片可读性：悬停提示改为「【主分类】· 标签…」；未设主分类的卡片加虚线红框 + 「未分类」小角标，一眼看出待补的图
- 全部为本地增量更新（PATCH 成功后改内存 + 刷新角标 + 菜单计数），不触发全量重拉
- 冒烟测试新增两项：`#cwCats` 面板渲染、**点主分类后真的写回图库**（本地 10/10 通过；期间还修掉了测试自身的一个坑：点击卡片后面板会重建 chips，必须重新取元素引用）

### 批次 20 · 修复滚动加载失效（v0.26.1，2026-09-16）🔧
- 现象（用户反馈）：底部显示「已加载 12 / 104 · 滚动加载更多…」，但滚动到底不再加载
- 根因：**IntersectionObserver 只在交叉状态变化时回调**。首屏 `observe()` 时照片数据还没加载完，回调里的 `appendMore()` 空转返回（`shown >= filtered.length` 都是 0）；此后哨兵一直停在"已交叉"状态，滚动时不再产生状态变化 → 回调永不触发。B 批把 scroll 监听换成哨兵时引入了这个回归
- 修复：
  - 新增 `maybeFillViewport()`：只要哨兵仍在「视口 + 800px 缓冲」范围内就继续追加，直到填满视口或数据加载完；在 `render()` 之后与每次追加之后调用（自己会递归补足，不会漏）
  - 保留观察器负责"滚动时触发"，并补一个 **rAF 节流的 `scroll` 兜底**（passive 监听，回调里只读一次 `getBoundingClientRect`，不会再像旧实现那样每次滚动读 `offsetHeight` 强制布局）
  - 顺带把 `sentinel` 的声明提前到 `initGallery()` 顶部（`let sentinel = null`），避免渲染时尚未初始化
- 测试增强：冒烟测试给哨兵**打桩一个可控的"距视口顶部"位置**来模拟滚动（`innerHeight` 固定 800），新增两项断言 —— 「哨兵在视口外时不会乱加载」、「滚动到底触发追加」；本地 11/11 通过（12 张 → 滚动后 30 张全部加载）

### 批次 21 · 整理视图记进度（v0.27，2026-09-16）✅
> 用户反馈：「整理」里已经点过『应用并下一张』的图片，刷新后又出现在待整理队列里。
- 根因有两层：
  1. **判定逻辑**：默认「待整理」的条件是「缺主分类 **或** 没有库内标签」（OR）。只设了主分类、还没打标签的图，应用后依旧符合条件，于是重算队列时又被选中
  2. **写入延迟**：Netlify Blobs 写入后读取有 1~2 分钟延迟，刷新得早会读到旧状态，看起来像"没保存"
- 修复：
  - 新增**整理进度**（localStorage `rn_rv_done`，保存已「应用」过的图片 id，上限 3000 条）：应用成功的图会被记下，`rvPickIds()` 在「待整理 / 未分类 / 无标签」筛选中直接跳过（「全部」筛选保留它们，便于回看）
  - 顶部新增「**重置进度**」按钮：清空记录，让所有符合条件的图片重新出现（想重新过一遍时用）
  - 进度显示「第 n / N 张 · 还剩 N 张 · 本轮已整理 M 张」，全部过完时提示累计数量
  - 顺手加一道保护：**主分类为空时不允许"应用"**（提示先选主分类或点跳过），避免空着手点应用把已有主分类/标签清掉
  - 跳过（S）不计入进度（只是暂时跳过）
- 测试：冒烟测试新增「整理视图有待整理项 / 应用后记入进度 / 应用后前进到下一张 / **重新加载后不再出现**」四项断言（本地 15/15 通过；期间修掉测试自身一个坑：点主分类 chip 时若该图已含此分类会变成"取消"，必须挑一个未选中的）

### 批次 22 · 主分类就地编辑（v0.28，2026-09-16）✅
> 用户反馈：「主分类允许编辑」—— 分类工作台 / 整理视图里的分类名是死的，改名、改色、增删只能去「标签 → 分组」页操作。
- 通用能力：`renderCatPicks()` 支持容器 `data-can-edit="1"`，每个分类名右侧渲染一个「✎」入口；`bindCatPicks()` 的点击委托优先识别 `.cat-edit`（`preventDefault` + `stopPropagation`），**不会影响当前选中状态**
- 分类工作台（标签 → 分类）：主分类面板标题右侧加「**＋ 新建**」，分类名带 ✎（独立于 `cwApplyCategory` 的点击处理，同样先拦截 ✎）
- 整理视图（标签 → 整理）：同样加「＋ 新建」与 ✎
- 编辑弹窗复用既有 `openCatModal("edit-category" / "new-category")`：改色、改名（**改名会同步所有照片的 categories**）、删除（清空引用）都已具备；保存后自动刷新管理视图与图库墙
- 未给上传页 / 编辑弹窗 / 批量弹窗的分类 chips 加 ✎（那些场景是"选分类"，不需要就地编辑，保持点击区域干净）
- CSS：`.cat-pick .cat-edit`（悬停浮现）、`.uq-panel-title .mini-link`（标题旁小按钮）
- 测试：冒烟测试新增 3 项断言 —— 「主分类带编辑入口 / 点 ✎ 打开编辑主分类弹窗 / 点＋新建打开新建主分类弹窗」（只验证弹窗打开，不改线上数据）；本地 18/18 通过

### 批次 23 · 标签组折叠（v0.29，2026-09-16）✅
> 用户反馈：「标签管理的分组部分允许有大量标签的标签组折叠」—— 分组视图把每组标签全铺成 pill，原神组 103 个标签一次全展开。
- 分组视图（标签 → 分组）里每个组头**可点击折叠 / 展开**：
  - **默认规则**：标签数 > 20 的组自动折叠（大组不再一次铺开几百个 pill，DOM 也小很多）；小组保持展开
  - 折叠状态记进 localStorage（`rn_tmgr_fold`，形如 `open:组id` / `fold:组id`，未分组用 `__free`）→ 手动展开过的组刷新后仍是展开的
  - 折叠时组头右侧计数变成强调色，箭头旋转；「未分组 · 待整理」区同样支持折叠
- 顶部新增「**全部折叠**」「**全部展开**」两个快捷按钮（仅分组视图显示）
- 组头的编辑按钮（✎）不触发折叠（事件里先判断 `closest("button")`）
- 测试：冒烟测试新增 5 项断言 —— 「分组视图有可折叠组头 / 标签多的组默认折叠 / 点组头可展开 / 全部折叠生效 / 全部展开生效」；本地 23/23 通过（用导入的 224 标签库验证，原神组 103 个标签默认折叠）

### 批次 24 · 组头直接新建标签（v0.30，2026-09-16）✅
> 用户反馈：「新建标签要放在标签组的旁边，方便新建标签」—— 原先只能点底部「＋ 新建标签」，再到弹窗里选所属组。
- 分组视图每个组头右侧加「**＋**」按钮：点它直接**在该组**新建标签（弹窗里所属组已预选好，标题也会标明「→「原神」组」），名称输入框自动聚焦
- 「未分组 · 待整理」区同样有「＋」（新建到未分组）
- 组头按钮区做成一对：`＋`（新建标签到本组）与 `✎`（编辑组）；点它们**不会触发组头折叠**（`stopPropagation` + 折叠处理里先判断 `closest("button")`）
- `openTagModal()` 新增第 4 个参数 `presetGroup`（默认预选所属组），底部「＋ 新建标签」行为不变
- 底部原有按钮全部保留（新建主分类 / 新建标签 / 新建标签组 / 批量导入）
- 测试：冒烟测试新增 3 项断言 —— 「组头有＋入口 / ＋打开新建标签弹窗且标题标出目标组 / 所属组已预选」；另把「滚动追加」断言改为**按数据量自适应**（照片不足首屏时改验进度提示，避免误报）；本地 26/26 通过

### 批次 25 · 改名/删除后界面立即更新（v0.31，2026-09-16）✅
> 用户反馈：「修改标签的名字后，无法快速的更新」。
- 根因：改名 / 删除走的是「调接口（内部 `loadTags()` 重读配置）→ 再 `loadData()` 重读照片」的路径，
  而 **Netlify Blobs 写入后读取有 1~2 分钟延迟**，重读会拿到旧数据 → 界面长时间显示旧名
- 修复思路：**后端写入 + 前端本地立即生效**，不再依赖服务端重读
  - `apiRenameTag` / `apiRemoveTag` / `apiRenameCategory` / `apiRemoveCategory` 去掉内部 `loadTags()`
  - 新增本地更新helper：`renameTagLocally()`（含"目标已存在=合并"的处理）、`removeTagLocally()`、`renameCategoryLocally()`、`removeCategoryLocally()` —— 同时改 `TAGS` 与 `PHOTOS` 中的引用
  - 改名 / 删除标签、改名 / 删除主分类四处调用点全部改为「发请求 → 本地更新 → `refreshTagUI()` + 重绘图库墙」，去掉 `loadData()` 重读
  - 新建类操作本来就是"写后返回服务端归一化结果"，不受影响
- **顺带修掉一个相关缺陷**：分组视图此前只显示「照片里有但库里没有」的游离标签，**库里"未分组"的标签根本不显示** —— 新建一个不选组的标签后会以为"没保存/没更新"。现在未分组区合并展示「库内未分组 + 游离标签」
- 测试：冒烟测试新增 3 项断言（用一次性临时标签，最后自动删除）—— 「新建的未分组标签能在分组视图看到 / **改名后界面立即更新（不等服务端）** / 临时标签已清理」；实测改名后 522ms 界面已更新；本地 29/29 通过

### 批次 26 · 相册窗口（v0.32，2026-09-16）✅
> 用户需求：悬浮按钮（FAB）上方加「相册」按钮，点击右滑进入相册界面；同时删掉页面切换菜单里的「图库」项；相册界面要左右两栏一屏。
- `index.html`：
  - FAB 的**上分支最上面**新增相册按钮（`#fabAlbumsBtn`，照片图标），原「切换页面」按钮仍在其下方
  - 页面切换菜单**删除「图库」项**（返回图库用各窗口右上角的「返回图库」按钮）；`data-page="gallery"` 的 closeAll 分支保留
  - 新增相册窗口 `#panelAlbums`：左栏相册列表 + 新建输入框，右栏相册内容
- 打开动画：`openWindow("albums")` 走**从右侧滑入**（`#panelAlbums.slide-in` + keyframes），其余窗口仍是右下角 genie 展开；关闭仍走原有吸入动画
- `app.js` 新增相册视图：
  - `renderAlbumsView()`：左栏相册行（名称 / 张数 / ✎ 重命名 / × 删除，与 PHOTOS 求交集计数）；右栏当前相册照片网格（点击进灯箱、✕ 移出相册）
  - `initAlbumsView()`：新建相册（回车）、切换、重命名、删除（确认弹窗）、移出图片后同步刷新筛选菜单与图库墙
  - 打开窗口（含重复置顶）时自动刷新视图；`window.__albumsState` / `__reloadAlbums` 作为测试钩子
- 样式：`.alb-split` 左右两栏（左 210px、右自适应）、窄屏（≤860px）自动上下堆叠、相册行与网格卡片样式
- 测试：冒烟测试新增 5 项断言 —— 「FAB 有相册按钮 / 页面菜单已移除图库项 / 点击后相册窗口打开 / 左栏有新建输入框 / 新建相册出现在左栏 / 右栏显示相册内图片」（线上自动跳过创建相册，避免动真实数据）；本地 35/35 通过

### 批次 27 · 改名/删除接口宽容化（v0.33，2026-09-16）✅
> 线上冒烟测试暴露：**刚新建的标签立刻改名会失败**（弹窗停在打开状态）。原因是改名/删除接口在服务端要「先读配置」判断存在性，而刚写入的配置还没传播 → 服务端读到旧副本 → 返回「标签不存在」。
- 服务端（`photos.js`）改为宽容语义：
  - `tagsRename`：配置里暂时看不到旧名时**不再 404**，改为「确保新名存在于配置 + 照常改写照片引用」，前端随后会用完整配置 PUT 覆盖对齐
  - `categoryRename`：同理（看不到旧名时补上新名，照片引用照常改写）
  - `categoryRemove`：配置里找不到该分类也继续清理照片引用（不再直接报错）
- 前端：删除标签 / 删除主分类后再**补一次 `apiSaveTags()`**，用本地（已删）的完整配置覆盖服务端，避免读旧副本把刚删的标签带回来
- 测试脚本同时修掉三处自身问题：改名/删除改为**轮询等待**（线上要发两个请求，原先固定 500ms 太短）、点组头 ＋ 前先刷新面板（避免取到被重建的旧元素）、清理断言改为轮询确认
- **最终线上冒烟测试 33/33 通过**（含「改名后界面立即更新」1744ms、「临时标签已清理」），本地同样全通过

### 批次 28 · 相册整页改版（iOS 相册风格，v0.34，2026-09-16）✅
> 用户澄清：相册应该是**左滑切换的另一个整页**（像顶部导航栏切页），设计参考**苹果手机图册**；上一版的"悬浮窗口 + 左右两栏"不符合预期，本次重做。
- `index.html`：删除悬浮窗口 `#panelAlbums`，新增整页 `#albumPage`：
  - 顶部**导航栏**（返回按钮 + 标题 + 新建按钮，`sticky` 常驻，毛玻璃背景，性能模式下自动降级为纯色）
  - 「＋」展开的新建相册输入行；内容区 `#albumBody`
- `app.js`：
  - `openAlbumPage()` / `closeAlbumPage()`：整页进出（`translateX(100%) → 0`，0.42s 缓出），并锁住背景滚动（`body.album-open`）
  - **两级视图**（iOS 相册逻辑）：一级 = 相册**封面网格**（封面取相册内最新一张、名称、张数；无图显示占位图标）；二级 = 该相册的**照片网格**（细密方格、点图进灯箱、✕ 移出、重命名 / 删除相册）
  - 返回按钮与手势分层：二级 → 一级 → 关闭相册页
  - **手势**：相册页内**右滑**返回（二级先回一级）；图库页**右边缘 60px 内左滑**进入相册（限定边缘起手，避免与卡片拖拽/页面滚动冲突）；桌面端 `Esc` 返回上一级
  - 打开相册页 / 收起悬浮菜单互斥处理；FAB 相册按钮直接调用 `openAlbumPage()`
- `style.css`：iOS 风格 —— 导航栏（54px、毛玻璃）、**大标题**（30px 粗体 + 计数）、封面网格（2 / 4 / 6 列自适应）、照片网格（3 / 5 / 7 列）、空态文案；旧的左右两栏样式整块移除
- 悬浮窗口体系里的相册相关映射（panels / openWindow 的 albums 分支）一并清理，避免指向已删除节点
- 测试：冒烟测试相册部分重写为 6 项断言 —— 「点击后相册整页打开 / 有顶部导航栏与大标题容器 / 点＋展开新建输入框 / 新建相册出现在一级网格 / 点封面进入二级并显示照片 / 返回按钮分层正确（二级→一级→关闭）」；线上只跑前 3 项（跳过创建，避免动真实数据）；本地 38/38 通过

### 批次 29 · 移除收藏、改为「加入相册」（v0.35，2026-09-16）✅
> 用户需求：收藏功能换成加入相册，并把原本关于收藏的代码全部删除。
- **删除的收藏代码**（前端 localStorage 方案，与后端无关）：
  - 状态与工具：`FAV_KEY` / `favs` Set / `loadFavs()` / `saveFavs()` / `isFav()` / `toggleFav()`，以及启动时的 `loadFavs()` 调用
  - 卡片右上角**收藏星标**（`.fav-star` 按钮、`favSVG` 图标）与 grid 点击里的星标分支
  - 灯箱工具条的**收藏按钮**（`#lbToolFav` 及其处理）
  - 筛选菜单的**「收藏」行**（`data-tag="__fav"`）与 `basePred()` 里的 `__fav` 分支
  - CSS：`.card .fav-star` 全套样式、`.lb-tool.fav-on`、`body.select-mode .card .fav-star`
  - 词典里的「收藏": "Favorites" 条目；字典/注释里的收藏措辞一并清理
- **替代实现「加入相册」**：
  - 卡片右上角改为**相册按钮**（`.alb-add`）：点击打开既有的相册选择弹窗（`openAlbumPicker([id])`）；**已在相册中的图片按钮常显高亮**（悬停提示显示它在哪些相册里），方便一眼看出
  - 灯箱侧栏本来就有「加入相册」按钮（`#lbToolAlbum`），收藏按钮删掉后不损失能力
  - 批量操作条的「＋ 入相册」、筛选菜单的相册区（各相册 + 未入册）保持不变
  - `addToAlbum()` 保存成功后额外刷新相册页（相册页开着时封面与张数即时更新）
- 注：老的 `rn_favs` 数据不再被读取（保留在浏览器里不影响任何功能，需要时可自行清除）
- 测试：冒烟测试新增 5 项断言 —— 「卡片有加入相册按钮 / 卡片不再有收藏星标 / 灯箱不再有收藏按钮 / 筛选菜单不再有收藏行 / 点卡片按钮打开相册选择弹窗」；本地 43/43 通过

### 批次 30 · 移除卡片上的快捷按钮（v0.36，2026-09-16）✅
> 用户要求：把卡片上的「加入相册」按钮也去掉（卡片保持干净）。
- 删除 `cardHTML()` 里的 `.alb-add` 按钮、`albumSVG` 图标常量、`albumsOfPhoto()` helper、grid 点击里的 `.alb-add` 分支
- 删除 CSS：`.card .alb-add` 全套（含 hover 显现、`.on` 高亮、`body.select-mode` 隐藏规则）
- 「加入相册」入口仍保留在两处：**灯箱侧栏按钮**（`#lbToolAlbum`）与**批量操作条「＋ 入相册」**；也可以从相册页二级视图里查看与管理
- 测试：断言改为「卡片不再有加入相册按钮」（同时保留「卡片不再有收藏星标」）；本地 42/42 通过

### 批次 31 · 灯箱只保留看图（移除信息面板与工具条，v0.37，2026-09-16）✅
> 用户要求：删除灯箱的 `<aside class="lb-info">` 整个模块。
- `index.html`：删除 `#lbInfo`（工具条 5 个按钮 + 描述 + 元信息 + 标签）与图片区的「显示/隐藏信息」悬浮按钮（`#lbToolInfo`）；灯箱只剩图片 + 关闭 / 上一张 / 下一张
- `app.js` 清理随之失效的代码：
  - `openLightboxById()` 里对 `lbDesc / lbDate / lbSize / lbDims / lbFormat / lbTags / lbToolInfo` 的写入全部移除
  - 灯箱工具条处理（播放 / 编辑 / 旋转 / 下载 / 加入相册）与**幻灯片放映**整块删除（`startSlide / stopSlide / toggleSlide / armHideTools`、空格键绑定、`window.__stopSlide`、打开灯箱时复位逻辑）
  - 全局 Esc 与灯箱关闭处的 `stopSlide()` 调用一并清掉
  - 设置页「幻灯片间隔」选项与快捷键说明里的「空格 幻灯片播放/暂停」删除，`initSlideSetting()` 及其调用移除
- `style.css`：用脚本按规则清理 —— 删除 33 条死规则、修剪 4 条多选择器规则（去掉仅服务 `.lb-*` 的选择器、保留仍在用的），再手工清掉 4 条残留（含媒体查询内），最终 `.lb-info/.lb-tool/.lb-tags/.lb-desc/.lb-meta` 引用 **0 残留**、括号配平校验通过
- **副作用与替代入口**：删掉工具条后「编辑图片」弹窗一度失去入口，因此把编辑入口移到**双击卡片**（单击看图 / 双击编辑，卡片 tooltip 已注明）；「加入相册」仍可从批量操作条进入；下载与旋转暂无入口（需要的话可再挂到别处）；后端 rotate 用的 `/api/photos/:id/image` 端点保留未动
- 测试：断言更新为「灯箱不再有信息面板 / 不再有工具条按钮 / 不再有收藏按钮 / 筛选菜单不再有收藏行 / 双击卡片打开编辑弹窗」；本地 43/43 通过

### 批次 32 · 灯箱右下角悬浮工具条（v0.38，2026-09-16）✅
> 用户要求：把原本的工具栏放到**右下角**、通过**悬浮显示**（上一批把工具条整块删了，本次按新形态恢复）。
- `index.html`：灯箱内新增 `.lb-tools-float`（`#lbToolsFloat`），5 个按钮 —— **幻灯片 / 编辑 / 旋转 / 下载原图 / 加入相册**；另恢复设置页「幻灯片间隔」（3s / 5s / 8s）与快捷键说明「空格 开始 / 暂停幻灯片」
- `app.js`：
  - **显隐机制**：工具条默认隐藏（`opacity:0` + `pointer-events:none`），鼠标在灯箱内移动 / 触摸 → 浮现（`.lightbox.tools-visible`）；**静止 3 秒自动淡出**；指针停在工具条上时保持显示；关闭灯箱一并收起
  - 切图逻辑抽成全局 `lbStep()` + `window.__lbStepList`（图库页注册"当前筛选后的顺序"），灯箱箭头、方向键、幻灯片、`window.__stopSlide` 共用同一套顺序
  - **幻灯片恢复**：`startSlide / stopSlide / toggleSlide`（间隔读 `rn_slide`）、按钮切换播放/暂停图标、空格键开关（仅在**可见**输入框内让位给打字）、`initSlideSetting()` 绑定设置页
  - **旋转**：本地 canvas 旋转 90° → `dataBase64` + `thumbBase64` 覆盖原图（`POST /api/photos/:id/image`）→ 重拉数据 + 带时间戳重开灯箱绕缓存
  - **下载**：`<a download>` 指向原图，文件名取标题（非法字符替换）+ 按 mime 补扩展名
  - 关闭灯箱统一走 `closeLightbox()`（Esc / ✕ / 删除图片后），都会停幻灯片 + 收工具条
- `style.css`：右下角胶囊工具条（毛玻璃、`24px` 边距、按钮 40px 圆形 hover 放大）；窄屏（≤900px）缩小到 36px；性能模式下取消毛玻璃与过渡
- 保留上一批的**双击卡片编辑**入口（两处入口并存，互不影响）
- 测试：断言更新为「工具条含 5 个按钮 / 默认隐藏 / 打开灯箱后仍隐藏 / 鼠标移动后浮现 / 空格开始幻灯片（按钮切暂停态）/ 再按空格停止 / 关闭灯箱并收起工具条」；本地 45/46 通过（未通过的 1 项是「存在标签数 >20 的组」，需本地先导入角色标签库，与本次改动无关）
- 验证边界：旋转的 canvas 环节依赖真实浏览器（jsdom 无 canvas），本地只验证了后端覆盖端点接受该请求体（`200 ok 675x900 image/webp`）；下载同理未在 jsdom 中断言

### 批次 33 · 改名 / 删除改为乐观更新（v0.39，2026-09-16）🔧
> 起因：v0.38 的线上冒烟测试里「改名后界面立即更新」耗时 **4128ms** 后超时失败（v0.31 时代同项是 1744ms），而服务端其实改名成功（事后在标签配置里能看到新名）—— 说明界面仍在**等服务端两个请求返回后才刷新**，Blobs 一慢就卡住。用户最初的抱怨（"修改标签的名字后无法快速更新"）正是这个手感。
- 四处改动（标签改名 / 标签删除 / 主分类改名 / 主分类删除）统一为**乐观更新**：
  - 先在本地改（`renameTagLocally` / `removeTagLocally` / `renameCategoryLocally` / `removeCategoryLocally`）并**立即 `refreshTagUI()` + 重绘图库墙** → 界面 0 延迟响应
  - 再发服务端请求同步照片引用；**失败则本地回滚**（改名反向改回、删除走 `loadTags()` 拉回真实配置）并把错误显示在弹窗里
  - 成功路径仍保留 `apiSaveTags()`（用本地完整配置覆盖服务端，避免读延迟把旧名字带回来）
- 效果：本地冒烟测试「改名后界面立即更新」**17ms**（改前本地约 220ms、线上 4128ms 超时）
- 顺带清理线上历史遗留的 4 个冒烟测试临时标签（`zz测试a8c4 / f9x6改 / 7rru改 / rue4改`，均 200 ok）；读取仍受 Blobs 1~2 分钟传播延迟影响，需要等待后复查
- 测试：本地 45/46（未通过的仍是本地数据依赖项「存在标签数 >20 的组」）；线上复跑后确认

### 批次 34 · 冒烟测试清道夫（v0.40，2026-09-16）🔧
> 起因：线上标签库里反复出现 `zz测试*` 残留标签（`a8c4 / f9x6改 / 7rru改 / rue4改 / lam5改`），而且**每次读到的残留不一样、数量还不减**。
- 根因：冒烟测试会新建 → 改名 → 删除临时标签，而改名/删除后前端 `apiSaveTags()` 是**全量 PUT 配置**。若此时 `loadTags()` 读到的是**含历史残留的旧副本**（Blobs 写后读 1~2 分钟延迟），PUT 就把这些残留**写回**配置里 —— 于是一边删一边复活
- 修法：测试结尾加**清道夫** —— `GET /api/tags` → 过滤掉所有 `zz测试*` → `PUT` 覆盖一次，保证任何一次跑完都不留垃圾（同时兜住"本次临时标签因延迟没清干净"的情况）
- 线上实测：一次测试跑完清道夫清掉本次残留 `zz测试xhue改`；单独用一次性脚本连续 6 轮「读 → 过滤 → PUT（间隔 45s）」把历史残留清到 0，标签库回到 **223 个**（原 224 - 早期人工调整，无人工标签损失）
- 最终线上冒烟测试 **46/46 全部通过**（含「改名后界面立即更新 118ms」「临时标签已清理」「清道夫：清掉历史残留测试标签」）

### 批次 35 · 移除「描述」字段（v0.41，2026-09-16）✅
> 用户要求：删除所有的描述部分的代码。
- 前端（`index.html` / `app.js`）：
  - 编辑弹窗（`#edDesc`）与上传项编辑弹窗（`#uqDesc`）的「描述」文本域整块删除；`openEditModal` / `openUqEdit` 不再读写描述
  - 保存路径不再发送 `desc`：单张 PATCH 与上传（`xhr.send`）都去掉该字段；上传行「已编辑」标记改为只看标签 / 主分类
  - 搜索：筛选逻辑删除描述匹配（原 `p.title + " " + p.desc` 还会因 `desc` 为空产生 "undefined" 干扰），只按标题 / 标签 / 主分类；搜索框 placeholder 与提示文案同步改为「标题、标签」
  - 文案与词典：上传行 ✎ 提示、灯箱「编辑」按钮提示、`I18N_DICT` 的「描述 / Description」条目一并去掉
- 后端（`photos.js`）：上传 / URL 导入不再写 `desc`；PATCH 不再接受 `desc`（`if (body.desc !== undefined)` 分支删除）；列表索引条目不再带 `desc`
- **数据未动**：已有照片 meta 里的 `desc` 文本保留在存储中（不显示、不返回、不再写入），若哪天要恢复描述功能，数据还在。需要彻底清掉可以再说
- 测试：新增 3 项断言 —— 「编辑弹窗不再有描述输入 / 上传项编辑弹窗不再有描述输入 / 搜索框文案不含描述」；本地 49/50（未通过项仍是本地数据依赖的「存在标签数 >20 的组」）

### 批次 36 · 放弃游戏/角色自动识别 + 图库查重（v0.42，2026-09-16）✅
> 用户先问「有没有本地大模型能识别是原神还是 R18」，讨论细节后决定：**不做游戏/角色识别**，把需求收敛成「图库里有相同或相似的图时提醒我」。
- **删除自动识别那一套**（探索性代码，从未实测）：
  - `scripts/local-tagger.js`（WD14 Tagger 本地打标，343 行 —— 模型与 `onnxruntime-node` 都没装过，一次也没跑起来）
  - `scripts/data/tagger-map.json`（英文标签 → 中文标签映射，仅服务该脚本）
  - `package.json` 的 `npm run tagger`
  - **保留**：`scripts/data/chars-*.json`（角色名单，是标签库的数据来源）与 `scripts/import-chars.js`（把名单导入标签库）—— 它们只产出标签、不参与「看图识别」；线上那 223 个标签也原样保留
- **新增图库查重（本次真正要的功能）**：上传时的重复提醒早就有（内容 sha1 + dHash），但**库内既有照片**只能在命令行跑 `find-duplicates.js`，界面上没有入口 —— 现在补上：
  - 入口两处：**筛选菜单底部**「🔍 查找重复 / 相似图片」+ **设置 → 数据维护**「查找重复 / 相似图片」
  - 扫描逻辑（纯前端，零后端改动）：分页拉全量索引（`mapPhoto` 后带 url / thumbUrl）→ **内容哈希 sha1 相同 = 完全相同**；**dHash 汉明距离 ≤ 8 = 相似**；相似用并查集取连通分量归组（A~B、B~C 归为一组），组内标注最小差异
  - 性能：dHash 预转两个 32 位整数 + popcount 位运算，几千张两两比较仍是亚秒级
  - 结果界面：每组带「完全相同 / 相似 · 差异 n/64」徽标 + 缩略图 + 尺寸 / 体积 / 上传日期；每张可「看大图」（进灯箱）或「删除」（确认弹窗）；组头一条「保留最新一张，删除其余 N 张」（确认后逐张删、失败逐条报错）
  - 处理后就地从结果中移除，组内剩 1 张自动消组；无结果时提示「✓ 没发现重复或相似的图片（已扫 N 张）」；测试钩子 `window.__dupState()` → `{scanned, groups, exact}`
- 测试：新增 5 项断言 —— 「筛选菜单有查重入口 / 点查重打开弹窗并完成扫描 / 扫描结果有状态钩子 / 查重弹窗可关闭」；本地 seed 特意放了**完全相同**的副本 + 一张 sharp 缩到 95% 的**相似**变体 → 实测「扫了 8 张，2 组（完全相同 1）」，相似分支同样被覆盖；本地 53/54（未通过项仍是本地数据依赖的「存在标签数 >20 的组」）；**线上 53/53 全通过**（线上查重：104 张 0 组重复，与命令行 `find-duplicates.js` 的历史结论一致）

### 批次 37 · 灯箱切换改为点左右区域 / 左右滑动（v0.43，2026-09-16）✅
> 用户要求：移动端删掉灯箱的切换按键，换成左右滑动或点左右区域切换；**桌面端也一样**，删按钮、点左右区域切换。
- 删除：`index.html` 里的 `‹`（`.lb-prev`）/ `›`（`.lb-next`）两个按钮；`app.js` 里对应的 `lbPrevEl / lbNextEl` 绑定；`style.css` 里 4 条位置规则（含窄屏媒体查询）—— CSS 中 `lb-prev / lb-next` 引用归零
- 新增 `initLightboxNav()`：
  - **点击分区**：以灯箱中线分界，点左半边 = 上一张、右半边 = 下一张；点击关闭按钮 / 悬浮工具条按钮不触发（`closest("button")` + 工具条容器判断）
  - **移动端左右滑动**：`touchstart → touchend`，横向位移 ≥ 45px 且明显大于纵向（×1.4）才算滑动；左滑 = 下一张、右滑 = 上一张；滑动后 400ms 内忽略尾随 click（避免「滑一次切两张」）
  - 键盘 `←` `→` 保留不变；窄屏下图片区显示 `cursor: pointer`
  - `style.css` 给灯箱加 `touch-action: pan-y pinch-zoom` —— 横向手势归我们处理，纵向滚动与双指缩放仍交给浏览器
- **顺带修掉一个手势冲突**：图库页「右边缘左滑进入相册」的手势监听在 `document` 上，灯箱里滑图会同时触发它 → 该手势现在遇到灯箱打开时直接忽略（`lbIsOpen()`）
- 设置页快捷键说明补一行：「点左 / 右 · 左右滑 —— 灯箱中点左 / 右区域或左右滑动切换上一张 / 下一张」
- 测试：新增 5 项断言 —— 「灯箱不再有上一张 / 下一张按钮 / 点灯箱左侧切到上一张 / 点灯箱右侧切回下一张 / 左滑切到下一张 / 右滑切回上一张」（滑动用自定义 `Event` + `changedTouches` 模拟）；本地 58/59（未通过项仍是本地数据依赖的那 1 项）

### 批次 38 · 灯箱点空白关闭、删掉 ✕ 按钮（v0.44，2026-09-16）✅
> 用户要求：点空白即可返回，不要 `✕` 关闭按钮。
- 删除 `index.html` 里的 `✕`（`.lb-close`）；`app.js` 里的 `lbCloseEl` 绑定；`style.css` 里全部 `.lb-btn` / `.lb-close` 规则（基础样式、hover / active、浅色主题覆盖、以及「灯箱控件 · 液态玻璃」整块）—— CSS 中 `lb-btn` / `lb-close` 引用归零，括号配平校验通过
- 点击语义改为**按目标区分**（这是本轮唯一需要小心的地方）：
  - 点**图片本身**（`#lbImg`）→ 按屏幕中线分左右半边：左半边上一张、右半边下一张（保留上一批的「点左右区域切换」）
  - 点**图片以外的任何空白**（图片区留白、灯箱 padding）→ `closeLightbox()` 关闭（顺带停幻灯片、收工具条）
  - 右下角悬浮工具条按钮照旧不触发这两者（`closest("button")` + 容器判断）
- 现在灯箱里没有任何按钮：切图靠点图片 / 左右滑动 / `←` `→`，关闭靠点空白 / `Esc`
- 光标提示：图片上是 `pointer`（可点），灯箱本身是默认光标（点它 = 关闭）
- 设置页快捷键说明补一条：「点空白 —— 关闭灯箱（点图片以外的空白处）」
- 测试：断言改为「灯箱不再有上一张 / 下一张 / 关闭按钮 / 点图片左半边切到上一张 / 点图片右半边切回下一张 / 点空白关闭灯箱并收起工具条」（点图片用 img 元素派发、点空白用灯箱元素派发来区分目标）；本地 58/59（未通过项仍是本地数据依赖的那 1 项）

### 批次 39 · 旋转只改预览、切图即复位（v0.45，2026-09-16）✅
> 用户要求：灯箱里的旋转只旋转灯箱中的图片，切到别的图后那张图自动恢复原样（也就是**不要**永久改原图）。
- 原实现是「canvas 本地旋转 90° → `POST /api/photos/:id/image` 覆盖原图 + 重算缩略图/哈希」，属于**破坏性**操作
- 新实现（`lbRotateView` / `applyLbRotation` / `resetLbRotation`）：
  - 点一次转 90°，只改灯箱里 `<img>` 的 `transform: rotate(Ndeg)` —— **不发任何请求、不动原图、不重算哈希**
  - 旋转 90° / 270° 时把图片的 `max-width` / `max-height` 反过来限制（`85vh` / `90vw`），避免竖图转横图后溢出屏幕
  - **切图即复位**：`openLightboxById()` 末尾调 `resetLbRotation()`，所以点图切换、左右滑动、`←` `→`、幻灯片自动前进、重新打开灯箱，都会回到正向
  - 按钮悬停提示改为「旋转 90°（仅预览，不动原图；切图即复位）」；`.lightbox img` 加了 `transform 0.25s` 过渡，转起来不生硬
- 后端 `POST /api/photos/:id/image`（覆盖原图字节）**保留但前端已无调用者** —— 需要真正永久旋转时还能用
- 测试：新增 2 项断言 —— 「点旋转 = 仅预览旋转 90°（`transform="rotate(90deg)"`）/ 切换图片后旋转自动复位（`transform=""`）」；本地 60/61（未通过项仍是本地数据依赖的那 1 项）

### 批次 40 · 正方形网格展示 + 一键切换（v0.46，2026-09-16）✅
> 用户要求：除了瀑布流，还要有「全部正方形」的排列（类似手机相册），切换按钮放在 FAB 左分支里（筛选 / 批量选择 / 排序 旁边）。
- `index.html`：FAB 左分支新增 `#fabLayoutBtn`，内置两个图标（`.ico-masonry` 三列高低块 / `.ico-square` 四宫格），按当前模式显示对应那个
- `app.js`：
  - `layoutMode()`（localStorage `rn_layout` = `square` / `masonry`，默认瀑布流）+ `applyLayout()`
  - 切换时给 `#grid` 加 / 去 `.grid-square`，按钮 title 在「切换为正方形网格展示 / 切换为瀑布流展示」之间切换，并调 `window.__fillViewport()` 把视口补满（方格一屏能放更多张）
  - 新增 `window.__fillViewport` 钩子（把 v0.26 的 `maybeFillViewport` 暴露出来），供布局切换后复用
  - 点击切换按钮 `stopPropagation()`，不会把 FAB 分支意外收起
- `style.css`：`.masonry.grid-square` 把 `columns` 换成 `display: grid` + `repeat(auto-fill, minmax(168px, 1fr))` + `gap: 4px`（手机相册那种紧密方格）；卡片 `aspect-ratio: 1/1 !important` 覆盖掉内联的原图宽高比、圆角收到 4px、`contain-intrinsic-size` 跟着改成方格高度（避免滚动时高度估算错）
  - 窄屏（≤700px）：`minmax(31vw, 1fr)` + `gap: 3px` → 约 3 列，接近手机相册
  - 悬停信息条（标签 / 日期）与选中、拖拽、双击编辑、多选等交互在两种模式下都照旧工作
- 测试：新增 5 项断言 —— 「FAB 分支有展示方式切换按钮 / 点一下切换展示方式 / 切换后卡片仍在（布局变化不影响渲染）/ 选择已写入 localStorage / 再点一下切回原样」；本地 65/66（未通过项仍是本地数据依赖的那 1 项）

### 批次 41 · 灯箱图片铺满 + 滚轮缩放（v0.47，2026-09-16）✅
> 用户要求：灯箱图片尽量铺满但不要变形；桌面端支持鼠标滚轮放大缩小。
- **铺满（不变形）**：
  - 灯箱内边距 40px → **24px**，`.lb-img-wrap` 由 `max-height: 85vh` 改为 `align-self: stretch` + `min-height/width: 0` → 图片可用区域 = 整个视口减去一圈内边距
  - `.lightbox img` 的 `max-height` 由 `85vh` 改为 **`100%`**，配合原有的 `object-fit: contain` —— 大图缩到边界、小图保持原尺寸（不放大、不模糊），比例始终不变
  - 窄屏媒体查询里同样是「图片 + 信息栏」时代的遗留（`max-height: 50vh`），一起改成铺满、内边距 10px（信息栏在 v0.37 就删了）
- **滚轮缩放**：
  - `lbZoom`（1 = 适屏）＋ `lbZoomBy(factor, originX, originY)`；滚轮上滚 ×1.18、下滚 ÷1.18，范围 **1× ~ 8×**，缩回 1× 即恢复适屏
  - **以鼠标位置为锚点**（把鼠标在图片内的百分比写进 `transform-origin`），放大后把鼠标移到别处再滚就能看到那一片
  - 旋转与缩放合成同一个 `transform`（`rotate(Ndeg) scale(N)`），互不覆盖；放大期间给 `<img>` 加 `.zoomed` 关掉过渡（V0.45 的 `transform 0.25s` 会让滚轮缩放有延迟感），缩回 1× 后恢复平滑
  - 换图即复位：`resetLbRotation` 扩展为 `resetLbView`（旋转 + 缩放一起清零），沿用 v0.45 的调用点（点图切换 / 左右滑动 / ←→ / 幻灯片 / 重开灯箱）
  - 滚轮只在**图片上**生效（`closest("#lbImg")`），点空白关闭、点图片切图的行为不变
- 测试：新增 5 项断言 —— 「CSS：灯箱图片按比例铺满（`max-height: 100%` + `object-fit: contain`）/ 滚轮向上放大图片 / 滚轮向下缩回 1×（适屏）/ 切换图片后缩放自动复位」（滚轮用 `MouseEvent("wheel")` + 自定义 `deltaY` 模拟）；本地 69/70（未通过项仍是本地数据依赖的那 1 项）
- **已知取舍**：放大后超出视口的部分靠「移动鼠标再滚」查看，**没有做拖动平移**；需要的话可以再加

### 批次 42 · 放大后按住拖动平移（v0.48，2026-09-16）✅
> 上一批的取舍被用户点名：放大后需要能按住拖动。
- `app.js`：
  - 新增 `lbPanX / lbPanY` 与 `clampLbPan()`；`applyLbView()` 的 transform 变成 `translate(px, py) rotate(Ndeg) scale(N)`（平移写在最前，所以它始终是**屏幕像素**、不受缩放影响）
  - **拖动**：`mousedown`（仅 `lbZoom > 1` 且落在图片上）→ `mousemove` 在 `window` 上跟随 → `mouseup` / `window.blur` 结束；`mousedown` 里 `preventDefault()` 挡掉浏览器原生图片拖拽
  - **拖动时把缩放锚点归中**（`lbCenterOrigin()`），同时按 `-(o - 0.5) × 尺寸 × (zoom - 1)` 补偿平移量 —— 否则从「鼠标锚点缩放」切到「中心缩放」时会跳一下
  - **边界限制**：可平移范围 = 图片视觉尺寸超出容器的一半（`(bw × zoom - cw) / 2`），拖到底就停，图片不会被拖出视口；量不到尺寸的环境（无布局）自动跳过限制
  - **移动端手势分层**：`lbZoom > 1` 时触摸 = 拖动平移（`touchmove` 里 `preventDefault()`，不让页面跟着滚），未放大时触摸仍是左右滑动切图；`touchend` 若刚拖过则不切图
  - 缩回 1× 时平移量一并清零；换图 / 重开灯箱走 `resetLbView()` 全清（旋转 + 缩放 + 平移）
  - 拖动结束后 300ms 抑制 click（鼠标拖动松手时不要顺手触发「点左右半边切图」或「点空白关闭」）
- `style.css`：放大时光标 `grab`、拖动中 `grabbing`（用 `.lightbox #lbImg.zoomed / .panning`，因为 v0.43 的 `.lightbox #lbImg { cursor: pointer }` 带 id、优先级更高）
- 测试：新增 6 项断言 —— 「放大后按住拖动 = 平移图片（`translate(60px, 30px) scale(1.18)`）/ 拖动松手不会顺手切图 / 拖动结束后图片仍在放大状态 / 缩回 1× 后平移量清零 / 放大后触摸拖动 = 平移图片 / 触摸拖动结束不切图」；本地 75/76（未通过项仍是本地数据依赖的那 1 项）











